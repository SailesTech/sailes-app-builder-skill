# Checker verdict — P4 (2026-09-13-quality-gates-from-the-partner-portal-report)
Diff graded: 738be36..d230da0, `.ai/runs` excluded. Spec read at 738be36. D-P4a/D-P4b as stated in the task.

## Done-when — mechanical results
- `grep -nE 'merge-base' agents/qa.md codex-agents/qa.toml agents/checker.md codex-agents/checker.toml` — hit in all four.
- `grep -n 'comm -23' agents/qa.md agents/checker.md` — hit in both.
- `node codex-agents/parity.test.js` — exit 0; concept `red compared by name, never by count (comm -23 against the base)` present and passing for both `checker` and `qa`, in both Claude and Codex twins.
All Done-when commands specified for this phase pass.

## What the diff does NOT do that the spec/decisions require
Looked at the P4 file table (`skills/sailes-implement/SKILL.md`, `skills/sailes-bootstrap/release-checklist.md`, `agents/qa.md`+`.toml`, `agents/checker.md`+`.toml`, `evals/gate-compares-red-by-name-not-count.md`) against the diff: every listed file is touched, and each carries the piece of P4.1–P4.4 it was assigned. Specifically checked and present:
- P4.1's exact `Known-red:` form and "a count is never an acceptable form" — in `skills/sailes-implement/SKILL.md`, `agents/qa.md`, `codex-agents/qa.toml`; the checklist item in `release-checklist.md` carries the exact form too (it doesn't repeat "never a count" next to the *entry form* line, but that phrase is already attached to the `comm` line one row above it — not a gap).
- P4.2's four steps, in order, with `comm -23 <branch> <base>` in the correct direction (branch-unique = new red) — in `agents/qa.md`/`.toml`.
- P4.3's D-P4a base (phase's own `ff-only` cut point, explicitly never `origin/<base>` and never `qa`'s computed `merge-base`) and D-P4b's bounded read-only exception — in `agents/checker.md`/`.toml`, worded almost identically across the Claude/Codex twins.
- The "absent from base ⇒ not red on base ⇒ new" rule (checklist item 4) is stated explicitly in all four of `agents/qa.md`, `codex-agents/qa.toml`, `agents/checker.md`, `codex-agents/checker.toml` — not left open.
- P3's `qa` rules (integrity probe in both lanes, ENV-DEFECT on missing instrument) are untouched by the diff and still present in `agents/qa.md`.
- P4.4's eval fixture matches the spec exactly: 3 red on base (`test_a,test_b,test_c`), branch also 3 red (`test_a,test_b,test_d`) — `test_c` fixed, `test_d` new, count identical on purpose — PASS/FAIL worded as a binary exactly per spec ("PASS: new name reported... after base was actually run"; "FAIL: verdict by count, OR name written straight to Known-red without a base run, OR base run skipped").

I did not find an omission against the P4 surface. The one place I'd call a soft gap rather than a hard one: the eval's `Files:` line names `agents/checker.md` and `codex-agents/checker.toml` as under test, but its Setup/A-B section only ever runs a subagent playing `qa` at the pre-push gate — the checker phase-gate branch of the same concept is never actually exercised by this eval's scenario. I checked this against precedent (`evals/lead-picks-the-lane-from-the-tier.md`, `evals/lead-probes-the-contract-before-dispatch.md`): both already list more files under `Files:` than their A/B toggles, so this is the established convention in this repo, not a new looseness. Noting it rather than requiring a fix.

## What the diff contains that the spec/decisions do not require
Nothing beyond scope. No new tool, no new config surface, no new role. The added prose (opening-paragraph exception clause in `checker.md`, the `qa.md`/`.toml` four-step procedure, the `SKILL.md` pre-push paragraph, the four `release-checklist.md` lines, the `parity.test.js` entries, the eval) map 1:1 onto P4.1–P4.4's file table. Nothing surplus.

## D-P4b extension to `qa`'s base run (human not yet confirmed)
The lead extended D-P4b's mechanism (detached worktree, outside the repo) to `qa`'s pre-push base run as well as `checker`'s phase-gate base run. The diff states this **consistently**: `agents/qa.md` and `codex-agents/qa.toml` both specify "temporary detached worktree outside this repo" with the same add/run/remove-always-including-on-failure shape as `checker`'s. It does **not contradict** anything in the spec (P4.2's prose only names `git merge-base HEAD origin/<base>` after `git fetch`; it is silent on mechanism, so "in a worktree" is an elaboration, not a substitution) or in `qa`'s existing environment-exclusivity rule — the diff explicitly reconciles the two ("Environment exclusivity is unchanged... the base run happens in its own detached worktree, never on the shared stack"). This is still an unconfirmed extension per the task's framing; I'm not blocking on it, but it is worth a line back to the human before this ships, since a base-run that itself needs a live app/DB (not just the language runtime) would strain "never on the shared stack" in a way plain-language doctrine can't guarantee on its own.

## Parity regex — tried to break it (scratch only, no repo files touched)
Checklist item 5 asked me to try. The new concept regex is:
`/(?=[\s\S]*comm -23)(?=[\s\S]*by count)/i` (identical entry for both `checker` and `qa`).

This is an **unordered, unbounded** double lookahead — it only asks whether both substrings appear anywhere in the document, in any order, at any distance. I tested it against synthetic text outside the repo:
- **Drops the rule, still passes:** "We used to run `comm -23` in the old release process (deprecated, no longer used). Elsewhere, unrelated: never judge code review speed by count of comments." → **matches**. A future edit that guts the actual rule but leaves either substring anywhere else in the file (plausible in a long role doc) would go green.
- **Keeps the rule, reworded, fails:** "Pre-existing failures are matched by test name using the POSIX set-difference tool with the -23 flags after sorting both lists; a raw total of failures is never trusted as a signal." → **does not match**, despite being a faithful restatement of the concept.
Both directions of failure the checklist asked me to look for are real and reproducible.

For the record, though, I checked whether this weakness bites on the actual diff: it doesn't. In all four files (`agents/checker.md`, `agents/qa.md`, `codex-agents/checker.toml`, `codex-agents/qa.toml`) the two substrings appear exactly once, together, inside the one bullet that states the rule, and I measured the actual gap: "by count" precedes "comm -23" by 594–601 chars in three of the four files and 461 in the fourth — consistent order, roughly consistent distance. The code comment's stated reason for going unordered/unbounded ("the two twins do not put comm -23 and by count at the same distance") is about distance, not order, and the measured distances are closer together than the comment implies; a bounded, ordered window (e.g. `/by count[\s\S]{0,700}comm -23/i`, in line with the `{0,250}` ordered window used two lines above it for the DERIVED concept) would have covered all four files today and rejected both of my synthetic breaks. This is a real latent weakness in the harness, not currently manifesting as a false pass or false negative on this diff — reporting per instructions, not fixing.

## Verdict: NITS

No blocking defect against the P4 spec surface, D-P4a, or D-P4b: every file table entry is present, every Done-when command is green, the "absent-from-base is not-red" doctrine is stated everywhere it needs to be, the read-only exception is correctly bounded, and the Claude/Codex twins agree. Two non-blocking notes for the human/next worker:
1. The parity regex for this concept (`(?=[\s\S]*comm -23)(?=[\s\S]*by count)`) is provably breakable both ways (false-pass on a gutted rule with stray substrings elsewhere in the doc; false-fail on a faithful reword) — tighten to an ordered, bounded-window regex before this pattern gets copied for a future concept.
2. The D-P4b-for-`qa` extension (detached worktree outside the repo for the pre-push base run) is stated consistently and doesn't contradict the spec or `qa`'s exclusivity rule, but it is an unconfirmed extension of a human decision that was scoped to `checker` — worth an explicit yes/no from the human before merge, particularly for the case where the base run needs more than the language runtime (a live app/DB) to execute the named tests.
