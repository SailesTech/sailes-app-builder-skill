# Spec: a probe field that names no block and no waiver

Status: approved

## Phase 1 — reads the deals endpoint

Contract-probe: verified against the deals endpoint, shape matches documentation

Done-when:
```
pnpm test src/deals → 0 failures
```
