P4 is complete. Final summary for the lead:

**Task:** Phase P4 of `.ai/specs/2026-09-13-quality-gates-from-the-partner-portal-report.md` — pre-existing red established by name against the base (Q7/R5/F3, plus decisions D-P4a/D-P4b).

**Base:** ff-only merged to `738be36` as instructed; worktree committed 6 checkpoints, final declaration commit `d230da0` (`impl(1.34.0-P4): pre-existing red established by name against the base`).

**Files changed** (all in scope, nothing extra):
- `/home/charlie/Work/Internal/sailes-app-builder-skill/skills/sailes-implement/SKILL.md` — Pre-push gate paragraph gains the red-by-name pointer; Pre-flight step 3 gains the `Known-red:` run-log section (exact entry form, count forbidden, expires at push).
- `/home/charlie/Work/Internal/sailes-app-builder-skill/skills/sailes-bootstrap/release-checklist.md` §0 — four checklist lines for the base run, `comm -23`, and `Known-red:`.
- `/home/charlie/Work/Internal/sailes-app-builder-skill/agents/qa.md` + `codex-agents/qa.toml` — full four-step procedure (sort branch red → run same names at merge-base in a temporary detached worktree → `comm -23` → `Known-red:`).
- `/home/charlie/Work/Internal/sailes-app-builder-skill/agents/checker.md` + `codex-agents/checker.toml` — same procedure for phase `Done-when` commands, base per D-P4a (the phase's own ff-only cut point, never `origin/&lt;base&gt;` or `qa`'s merge-base), plus a named, bounded read-only exception for the temporary worktree.
- `/home/charlie/Work/Internal/sailes-app-builder-skill/codex-agents/parity.test.js` — new concept "red compared by name, never by count" added to both `qa` and `checker` INVARIANTS, using order-independent lookaheads (flagged as a deviation from the file's usual fixed-distance-window regex style — see report §Ambiguities).
- `/home/charlie/Work/Internal/sailes-app-builder-skill/evals/gate-compares-red-by-name-not-count.md` (P4.4) — 3-red-on-base/3-red-on-branch fixture where the count stays equal but one name is genuinely new.

**Verification:** all five `Done-when` commands pass exactly as specified — both greps hit in all four role files, `parity.test.js` exit 0 with the new concept showing for both roles, `sync-blocks --check` in sync, `npm test` exit 0 across all 22 suites with 0 `not ok`. Mutation-proved the new parity concept on a scratch copy outside the repo (never `git checkout`, repo untouched): stripping the concept from `qa`'s twin gave exactly one FAIL on that concept; stripping it from `checker`'s twin gave exactly one FAIL on that concept; untouched copies passed both times.

**Ambiguities flagged in the report (not decided unilaterally):** the regex-style deviation above, and a note that no run-log *template* file exists in this repo to add a literal `Known-red:` heading to — I documented the section's existence/format in the skill prose instead.

Full report: `/home/charlie/Work/Internal/sailes-app-builder-skill/.claude/worktrees/agent-ae77fabd26176be4d/.ai/runs/2026-09-13-quality-gates-P4-be-dev-report.md`. Status file: `/home/charlie/Work/Internal/sailes-app-builder-skill/.claude/status/be-dev-P5ab-A2.md` (closed, outcome: done).
