# Docs delta — 1.34.0 (quality gates from the partner-portal report)

**Outcome: CONTENT UPDATED, RECEIPT BLOCKED.** One of the five diagrams (`architecture`) carries an
evidence-backed content edit for this release; it cannot be handed off with a passing
`deliver`/`compare` receipt this cycle, for the reason established in the 1.33.0 delta and left as
open backlog debt (see "Root cause" below) — **not caused by 1.34.0 content**. `workflow`,
`dataflow`, `sequence`, `lifecycle` carry **no content change** for 1.34.0 — evidence below is what
was checked to reach that conclusion.

## Archify

Present at session start: `~/.claude/skills/archify` reports `version: "2.17"` (floor 2.12).
`doctor` → 14/14 `[ok]`. Not a SKIP — the authoring loop ran in full against a live install.

## Source diff checked

`git diff --stat main...HEAD -- . ':!.ai'` → 42 files changed (confirmed against the task's own
count). Merge base confirmed: `git merge-base --is-ancestor fd03539 HEAD` → `ok`. Content read:
`tools/contract-probe-check.js` + its two test suites, `package.json` `test` script (now runs
`tools/contract-probe-check.test.js` and `tools/contract-probe-check.frozen.test.js`), `AGENTS.md`
§Verification (now names seven governance tools, up from six), `agents/{be-dev,fe-dev,checker,qa,
tester,team-lead}.md` + their `codex-agents/*.toml` twins, `codex-agents/parity.test.js` (new
invariants), `skills/sailes-bootstrap/gate-scaling.md`, `skills/sailes-pre-implement/SKILL.md`,
`skills/sailes-test/SKILL.md`, `skills/sailes-spec/SKILL.md`, three new `evals/*.md`.

## What changed, with evidence (`architecture.json`)

| Diagram | Node/card | Change | Evidence |
|---|---|---|---|
| architecture | `tools` component | tag `npm test — 7 scripts` → `npm test — 8 scripts` | `package.json:8` `test` script gains `node tools/contract-probe-check.test.js && node tools/contract-probe-check.frozen.test.js` — one new governance tool joins the six existing (sync-blocks, ownership-check, worker-status, mcp-toolnames-check, business-logic-check, deployed-surface-check) plus the separately-tracked token-report, matching the 6+1(token-report)=7 → 7+1=8 counting convention this node already used at 1.33.0 |
| architecture | "Framework Repo" card | new bullet for `tools/contract-probe-check.js`, trailing clause "seven governance tools in npm test, up from six" | `tools/contract-probe-check.js` (new, 288 lines) + `tools/contract-probe-check.test.js` (27 cases) + `tools/contract-probe-check.frozen.test.js` (41 cases, `CP01`–`CP41`); `AGENTS.md` §Verification: "the seven governance tools (`tools/{sync-blocks,ownership-check,worker-status,mcp-toolnames-check,business-logic-check,deployed-surface-check,contract-probe-check}.test.js` …)" |

No node, edge, boundary, or view was added or removed — only the `tools` node's tag and one card
bullet, the same class of edit the 1.30.0/1.33.0 precedent already tracks for a new `tools/*.js`
governance check.

## What was checked and found unchanged (`workflow`, `dataflow`, `sequence`, `lifecycle`)

- `workflow.json` — the `phase_gates` node stays generically "tester → checker → qa, all three,
  every phase": true in both the `full` and `middle` lanes this release adds (`agents/team-lead.md`
  gate-scaling block). The full-suite-once-before-push shift (`qa`, not per-worker-per-phase) and
  the lane split are verification-cadence and role-report mechanics one level of abstraction below
  what this diagram tracks — the same reasoning the 1.33.0 delta applied to `session-handoff`. No
  edit made.
- `dataflow.json` — `implementation-branch` sublabel ("be-dev / fe-dev implement, each in own
  worktree") and `npm-test-validators` ("hooks + TOML + frontmatter + release hygiene") stay
  accurate; this diagram does not name individual governance tools or report shapes at its
  abstraction level (it names categories: "npm test", "evals/"). `grep` for `contract-probe`,
  `Lane:`, `DERIVED`, `Done-when`, `middle lane` across the file returns nothing — an explicit
  empty delta, not an oversight. No edit made.
- `sequence.json` — "Closing a Spec (with the Docs-Delta Gate)": docs-author/archify/human
  actors and messages, unrelated to the phase-gate report-shape or lane changes. No edit made.
- `lifecycle.json` — spec draft → implemented/archived/superseded states; unaffected by 1.34.0.
  No edit made. This is also the only diagram that still **passes** `validate --quality showcase`
  (0 errors, 0 warnings) on this machine's archify 2.17.

## Root cause (pre-existing, not 1.34.0)

`composition/desktop-readability` (archify 2.17 floor) rejects `architecture`, `workflow`,
`dataflow` and `sequence` — reproduced on the **unmodified `main` base** (`fb69369`), confirmed
below, so this is the same open backlog row (`.ai/backlog.md`: "Re-layout
`docs/architecture/{architecture,dataflow,workflow,sequence}` for archify 2.17's
`composition/desktop-readability` floor … since 1.33.0 the JSON sources carry content the rendered
HTML pages do not show … human accepted as recorded debt"). The re-layout is explicitly out of
scope for this task.

## Commands run, exit codes, real output

- `archify doctor` → 14/14 `[ok]`, exit 0.
- `archify validate architecture docs/architecture/architecture.json --quality showcase` →
  `[composition/desktop-readability]` error, exit 1. Full JSON at
  `.ai/docs-deltas/2026-09-13-release-1.34.0-architecture-validate.json`.
- `archify deliver architecture docs/architecture/architecture.json docs/architecture/architecture.html --quality showcase`
  → `Final artifact check failed; the previous artifact was preserved.`, exit 1 (`architecture.html`
  on disk is unchanged from before this session — deliver refused to overwrite it, correctly).
- `archify validate workflow docs/architecture/workflow.json --quality showcase` → layout errors
  (tag-width overflow on `release_gate`, three `clean-flow/endpoint-side-direction` violations),
  exit 1 — pre-existing, no 1.34.0 content touches this diagram.
- `archify validate dataflow docs/architecture/dataflow.json --quality showcase` → layout errors
  (six sublabel-width overflows, one `clean-flow/endpoint-side-direction` violation), exit 1 —
  pre-existing, no 1.34.0 content touches this diagram.
- `archify validate sequence docs/architecture/sequence.json --quality showcase` → layout errors
  (two participant sublabel-width overflows), exit 1 — pre-existing, no 1.34.0 content touches
  this diagram.
- `archify validate lifecycle docs/architecture/lifecycle.json --quality showcase` → `ok lifecycle
  … (9 artifact checks; composition showcase: 0 errors, 0 warnings)`, exit 0.
- `archify compare architecture <main:architecture.json> docs/architecture/architecture.json
  --receipt <path> --json --quality showcase` → `"stage": "check"`,
  `"error": "Base snapshot failed validation: Validated snapshot failed final artifact checks."`,
  `composition/desktop-readability` on the **base** side, exit 1 — the base (unmodified `main`)
  fails the same floor check the head does, confirming this is pre-existing and not introduced by
  the `tools` node/card edit. Full JSON at
  `.ai/docs-deltas/2026-09-13-release-1.34.0-compare-attempt.json`. No receipt file was produced
  (compare never reached the stage that writes one).

## What the receipt does not have

- No passing `deliver` receipt (sha256 + bytes) for `architecture.json` this cycle — blocked by the
  pre-existing composition failure above, not by anything this release changed.
- No `compare` delta (added/removed/changed element list) — compare could not get past validating
  the base snapshot.
- `architecture.html`, `workflow.html`, `dataflow.html`, `sequence.html`, `lifecycle.html` on disk
  are **unchanged** — none of the four blocked diagrams' `.json` sources needed a content edit this
  release (workflow/dataflow/sequence/lifecycle carry an explicit empty delta), and `deliver`
  correctly refused to overwrite `architecture.html` with a failing artifact.

## Verification

- `git merge --ff-only feat/1.34.0-quality-gates` → fast-forward, `git merge-base --is-ancestor
  fd03539 HEAD && echo ok` → `ok`.
- `git diff --stat main...HEAD -- . ':!.ai'` → 42 files changed, 2023 insertions, 85 deletions —
  matches the task's stated count.
- `grep -n "contract-probe-check" package.json` → present in the `test` script, both the main and
  frozen suites.
- `node -p` version probe against `~/.claude/skills/archify/SKILL.md` → `"2.17"`, above the 2.12
  floor from `skills/sailes-docs/references/archify-setup.md`.
- Drift check across `workflow.json`, `dataflow.json`, `sequence.json`, `lifecycle.json`: grepped
  for `contract-probe`, `Lane:`, `DERIVED`, `Done-when`, `middle lane`, `full suite` — no matches in
  any of the four; each file's own abstraction level (generic phase/actor/state names, not
  per-tool or per-mechanic detail) is why the 1.30.0/1.33.0 precedent left them alone for
  comparable prior additions, applied identically here.
