# G8 review verdict — agents/checker.md "You never" fix

## Diff under review (fd4c72d..d2303e8)

```
--- a/agents/checker.md
+++ b/agents/checker.md
@@ ## You never
-- Touch or edit code — you are read-only. You may run lint/type/tests to confirm the machine's guarantees hold, nothing more.
+- Touch or edit code — you are read-only. You may run lint/type/tests to confirm the machine's guarantees hold; the one write beyond that is the named exception above — the temporary detached worktree for the base run, whose only write is `.git` worktree metadata.
```

`git diff --stat fd4c72d..d2303e8` → `agents/checker.md | 2 +-`, one file, one line changed. `codex-agents/checker.toml` is untouched by this diff, consistent with the spec's claim that the `.toml` twin already carries the exception.

## Commands run (paste)

`node codex-agents/parity.test.js` (in a temporary detached worktree at `d2303e8`, outside the repo, `git worktree remove --force` after):
- exit 0, all invariants pass, 10 roles both sides. Notably: `ok checker: "read-only" survives in BOTH twins` — parity treats the bare word "read-only" as an intentional, checked invariant string in both twins, not as a defect to remove.

`node agents/validate-frontmatter.test.js`:
- exit 0, `checker: frontmatter parses` / `required fields present` / etc. all ok, 10 role definitions valid.

`node tools/sync-blocks.js --check`:
- exit 0, "all blocks in sync".

## What the diff does NOT do that G8/G2 require

Nothing. G8's scope is exactly "lider zmienia jedną linię" (the lead changes one line) in `agents/checker.md`'s "You never" section so it no longer says "nothing more" while the same file already carries the named exception, and the `.toml` twin already carries it. That one-line edit is present, is the only change in the diff, and both `Done-when`-style checks I was told to run (parity, frontmatter validator) plus `sync-blocks --check` pass. There is no second file G8 asks this diff to touch — the `.toml` side of G8 is "already has it," not "needs a matching edit" — and the diff correctly leaves `codex-agents/checker.toml` untouched.

## What the diff contains that G8/G2 do not require

Nothing beyond the one bullet. The new clause adds one qualifying clause to the existing sentence and a back-reference ("the named exception above") rather than restating the full scope (base run only / `.git` worktree metadata only / never the diff / never the working tree) a second time in the same file. That's not scope creep — it is the minimal edit that removes the contradiction while relying on the "On read-only, honestly" paragraph a few lines above (`agents/checker.md:12`) to carry the full detail. No new files, no new commands, no new abstractions.

## Question 1 — any remaining unqualified read-only claim that contradicts the exception?

`agents/checker.md`:
- `agents/checker.md:3` (frontmatter `description`): "...Read-only; grades the artifact, not the story." — unqualified, but this is a one-line role tag, not a rule statement, and `parity.test.js` explicitly asserts the literal string "read-only" must survive in both twins as an invariant. Read together with the body (`:12`, `:37`), this is an **acceptable summary**, not a contradiction — the doctrine paragraph and the fixed bullet are where the actual constraint and its bounded exception live.
- `agents/checker.md:12` ("On read-only, honestly" paragraph): already states the exception in full (base run, temporary detached worktree outside the repo, only write is `.git` worktree metadata, never the diff, never the working tree). No contradiction — this is the source paragraph the fixed bullet points back to.
- `agents/checker.md:37` (the fixed "You never" bullet): now says "you are read-only... the one write beyond that is the named exception above." No longer says "nothing more." No contradiction.

I read the file whole and found no other sentence asserting unqualified read-only-ness — the "You do" section's verification bullets are consistent with running commands, not writing.

`codex-agents/checker.toml`:
- `codex-agents/checker.toml:3` (`description`): "Independent read-only Sailes review gate..." — same nature as the `.md` frontmatter: a short tag, unqualified on its own, but the body immediately below states the exception. **Acceptable summary**, not a contradiction, and it mirrors `.md:3` — consistent, not a stray inconsistency.
- `codex-agents/checker.toml:23`: "Remain read-only. You may run verification commands, but never edit code. Named exception: the base run above needs a temporary detached worktree outside the repo, and its only write is `.git` worktree metadata — never the diff under review, never the working tree you are grading." — already fully qualified, predates this diff, and is what G8 says the `.toml` "already carries." No contradiction.

No hit anywhere in either file, after the fix, that is a true contradiction (a sentence that forbids the exception or omits it while asserting absolute read-only-ness in a rule-bearing context). The two frontmatter `description` lines are the only unqualified strings, and they're headline tags reused verbatim as a checked invariant by `parity.test.js`, not doctrine statements — the same treatment on both sides.

## Question 2 — do the .md/.toml twins now say the same thing about the exception's scope?

Yes, and they say it in the same shape (a short rule line + a "Named exception" clause), even though the `.md` fix uses a back-reference instead of restating the scope:

- Base run only: `.md:12` "establishing pre-existing red (below) needs a base run"; `.toml:23` "the base run above needs a temporary detached worktree." Same.
- Location: `.md:12` "temporary detached worktree **outside** the repo (`git worktree add --detach`, then `git worktree remove`, always)"; `.toml:23` "temporary detached worktree outside the repo." Same.
- Only write: `.md:12` "the only write it makes is `.git` worktree metadata"; `.md:37` "whose only write is `.git` worktree metadata"; `.toml:23` "its only write is `.git` worktree metadata." Identical phrase, all three places.
- Explicit negatives: `.md:12` "never the diff under review and never the working tree you are grading"; `.toml:23` "never the diff under review, never the working tree you are grading" — near-verbatim. `.md:37` doesn't restate these two negatives inline, but it references "the named exception above," which is `.md:12`, where they're stated. So the scope is the same by composition, not divergent.

No mismatch in scope between the twins.

## Question 3 — does the diff change anything beyond that one bullet?

No. `git diff --stat` shows exactly one file, one line changed (`+1/-1`), inside `agents/checker.md`'s "You never" section. Nothing else in either file is touched.

## Question 4 — is the new bullet itself accurate and bounded, or readable as a general licence to write?

Bounded. "The one write beyond that is the named exception above" is singular ("the one write"), refers back to a single, already-fully-specified exception (not a category of exceptions), and that referenced paragraph (`:12`) itself bounds the write to "`.git` worktree metadata" only, taken via `git worktree add`/`remove`, "always," and explicitly excludes the diff under review and the working tree being graded. Nothing in the new wording opens a second door — it names exactly the one that G2 already named. Accurate: it correctly describes what `git worktree add --detach`/`remove` write (worktree administrative metadata under `.git`, not tracked file content).

## Verdict: APPROVE

The one-line fix does exactly what G8 asks — removes the "nothing more" phrasing that contradicted the named G2 exception already present in the same file and already present in the `.toml` twin — without touching anything else, without widening the exception, and without leaving the two twins in disagreement about its scope. `parity.test.js`, `validate-frontmatter.test.js`, and `sync-blocks.js --check` all exit 0 against the post-fix tree. The two remaining unqualified "read-only" strings (frontmatter `description` in both files) are short role tags rather than rule statements, are symmetric across both twins, and are pinned as an intentional invariant by `parity.test.js` itself — noted here as the honest answer to Question 1, not as a defect.
