# Docs delta — release 1.30.0

## What was verified against the repo (not trusted from the brief)

1. **Sixth governance tool.** `tools/deployed-surface-check.js` + `.test.js` confirmed present.
   `package.json` `test` script runs it (`node tools/deployed-surface-check.test.js`), between
   `business-logic-check.test.js` and `brief-closure.test.js`. `npm test` now runs 17 suites
   (counted from the script string), and `tools/*.js` now holds 6 governance scripts:
   sync-blocks, ownership-check, worker-status, mcp-toolnames-check, business-logic-check,
   deployed-surface-check. One-line purpose read from its own header
   (`tools/deployed-surface-check.js:1-31`): a spec phase that claims an HTTP status code, header
   or `Content-Type` must name a `Deployed-probe:` against a non-local address, or an explicit
   `n/a — <reason>` — the rule is "was the question asked", not "is the answer right".
2. **Pre-existing gap, confirmed and fixed in the same pass.** `docs/architecture/architecture.json`
   (component `tools`, and the "Framework Repo" card) named sync-blocks, ownership-check,
   worker-status, mcp-toolnames-check but not `business-logic-check` (shipped 1.29.0,
   `tools/business-logic-check.js` + `.test.js`, already wired into `npm test` before this
   release). This is a diagram already one tool behind, independent of 1.30.0's own change —
   reported as a finding **and corrected**, since a governance tool is the same node class already
   drawn on this diagram; leaving it out while adding the newest one would have shipped a diagram
   that was newly wrong in the same edit that fixed the old wrong.
3. **Third sync block confirmed.** `tools/blocks.json` lists `spec-weight`
   (`skills/sailes-bootstrap/spec-weight.md` → `skills/sailes-spec/SKILL.md`,
   `skills/sailes-bootstrap/spec-writing-template.md`, `skills/sailes-pre-implement/SKILL.md`)
   alongside `delegation-threshold` and `gate-scaling`. `node tools/sync-blocks.js --check` → "all
   blocks in sync" (exit 0). This is a data change to `sync-blocks`' inputs, not a new component —
   `architecture.json` already describes `tools/sync-blocks.js` generically ("stamps a rule from
   one canonical source into every consumer file"); I did not enumerate individual block names on
   the diagram (never did, for the other two blocks either) but did add the block count/names to
   the card bullet for traceability, since the count changed with this release.
4. **New "Phase 1b" in `skills/sailes-pre-implement/SKILL.md`** (lines 28-76) confirmed: runs
   between "Phase 1 — Load context" and "Phase 2 — Backward-compatibility audit", judges spec
   weight against the (now sync-blocked) `spec-weight` block and runs
   `node tools/deployed-surface-check.js <spec>`.

## Diagrams changed

- **`docs/architecture/architecture.json` — changed.** Two edits:
  - `tools` component sublabel: added `business-logic-check, deployed-surface-check` to the tool
    list; tag changed to `npm test — 6 tools`.
  - "Framework Repo" card: added a bullet each for `business-logic-check.js` and
    `deployed-surface-check.js` (one-liners sourced from their own file headers), noted
    `sync-blocks.js` now stamps three named blocks, and closed the tools bullet with "six
    governance tools in npm test, up from five" (five was correct going into this release —
    `sync-blocks`, `ownership-check`, `worker-status`, `mcp-toolnames-check`,
    `business-logic-check` — even though the diagram itself only showed four of those five before
    this pass; the drawn/actual gap is item 2 above, now closed).
  - Validated: `archify validate architecture … --quality showcase` → 9/9 checks, 0 errors, 0
    warnings.
  - Delivered: `archify deliver architecture docs/architecture/architecture.json
    docs/architecture/architecture.html --quality showcase` →
    spec sha256 `66159e43f7b4f273f968d44d0db456742a0ae5cda9f98fd8a7ef753dd990e0c8` (6812 bytes),
    artifact sha256 `8a279a75dee18e7174c396b7b29d63f7fccbea0b70ab875f3bd1cc79b6cc26ce` (633438 bytes),
    9/9 checks, 0 errors, 0 warnings.
  - Compare receipt (base = `git show HEAD:docs/architecture/architecture.json` at
    `76554a5be8243e9710313f10c49ad1a19cd0e661`, head = the edited file):
    `.ai/docs-deltas/2026-08-30-release-1.30.0.json` (HTML sibling gitignored per
    `.gitignore:33`, not committed). Summary: **components: 1 changed (`tools` —
    `/sublabel`, `/tag`), 0 added, 0 removed; connections: 0 changed; boundaries: 0 changed.**
    28/28 validation checks passed on both base and head compositions.

- **`docs/architecture/workflow.json` — left unchanged, decision recorded.** The pre-implement
  phase is drawn at phase granularity: the `pre_implement` node already collapses today's Phase 1
  / Phase 2 / Phase 3 sub-steps (BC audit, gap check, etc. — `skills/sailes-pre-implement/SKILL.md`)
  into one box labelled "BC + risk + gaps". The new Phase 1b (spec-weight judgment + wire probe)
  is a sub-step of that same phase, not a new phase in the pipeline and not a new gate — it does
  not change who runs it, what it hands off to, or where in the sequence Pre-Implement sits
  relative to Spec/Implement. Drawing it as a new node would be inconsistent with every other
  Pre-Implement sub-step already left undrawn on this diagram. My call: no change. (If the human
  disagrees and wants Phase 1b surfaced as its own node/tag, that is a one-line addition to
  `pre_implement`'s sublabel — flagging it rather than silently deciding it is final.)

- **`docs/architecture/sequence.json` — left unchanged, not affected.** This diagram documents the
  docs-delta gate sequence itself (team-lead / docs-author / archify-cli / human), unrelated to the
  pre-implement gate's internal steps. None of this release's four changes touch that flow.

- **`docs/architecture/dataflow.json` — left unchanged, not affected.** Documents brief→spec→
  branch→release→deploy→memory at the framework-repo level; none of the four changes alter a
  stage, a node, or a flow at that granularity (the new tool and sync block are internal to stages
  already drawn as "npm test" / "Feature Branch", and the readiness sub-step doesn't appear at this
  level either).

- **`docs/architecture/lifecycle.json` — left unchanged, not affected.** Documents a spec's own
  state machine (draft → approved → in-progress → docs-delta-gate → implemented/superseded/
  archived); none of the four changes alter a state or transition in that machine.

## SKIP status

No SKIP fired. Archify 2.12 (at floor) is installed at
`C:/Users/karol/.claude/skills/archify` (symlinked from `~/.agents/skills/archify`); `doctor`
reported all green; `validate`/`deliver`/`compare` all ran and returned exit 0 / `"ok": true`.

## Verification — `npm test`

Ran from repo root, full output captured; exit code below is the real process exit, not inferred
from text matches.

```
> sailes-app-builder-skill@1.30.0 test
> node tools/sync-blocks.test.js && node tools/ownership-check.test.js && node tools/worker-status.test.js && node tools/mcp-toolnames-check.test.js && node tools/business-logic-check.test.js && node tools/deployed-surface-check.test.js && node skills/sailes-bootstrap/hooks-template/brief-closure.test.js && node hooks/workflow-router.test.js && node hooks/framework-version-check.test.js && node codex-agents/validate-toml.test.js && node codex-agents/parity.test.js && node evals/harness/eval-status.test.js && node agents/validate-frontmatter.test.js && node release-hygiene.test.js && node spec-status-evidence.test.js && node skills/sailes-bootstrap/repo-done-checklist.test.js && node skills/sailes-bootstrap/hooks-template/hooks-template.test.js

sync-blocks: all tests passed
ownership-check: all tests passed
worker-status: all tests passed
...
release-hygiene: all tests passed (five stamps at 1.30.0)
  ok   the self-docs set is present — five sources, five rendered pages
spec-status evidence: all tests passed
repo-done-checklist: all tests passed
hooks-template: all tests passed

EXIT: 0
```

No `not ok` / failure lines anywhere in the run (checked with a case-insensitive scan for
`not ok`, `FAILED`, `✗`, ` fail:` — zero matches). `release-hygiene.test.js` explicitly asserted
the self-docs set is present, at version 1.30.0.

## What could NOT be established from evidence

- No `graphify-out/graph.json` exists in this repo, so the evidence order fell to direct code
  reads (package.json, `tools/*.js` headers, `tools/blocks.json`,
  `skills/sailes-pre-implement/SKILL.md`) rather than a graphify query — stated per
  `authoring.md`'s evidence order, not a gap in the finding itself.
- Whether the human wants Phase 1b surfaced on `workflow.json` is a judgment call I made, not a
  fact I verified; recorded above so it can be overridden.

## Files delivered / touched

- `docs/architecture/architecture.json` — edited (component + card).
- `docs/architecture/architecture.html` — regenerated via `deliver` (sha256
  `8a279a75dee18e7174c396b7b29d63f7fccbea0b70ab875f3bd1cc79b6cc26ce`).
- `.ai/docs-deltas/2026-08-30-release-1.30.0.json` — compare receipt (committed candidate;
  authoring agent does not commit).
- `.ai/docs-deltas/2026-08-30-release-1.30.0.html` — rendered delta, gitignored, not for commit.
- `docs/architecture/workflow.json`, `sequence.json`, `dataflow.json`, `lifecycle.json` — untouched,
  byte-identical to base; reasons above.

## Status

Task complete for this pass: one diagram changed with full validate+deliver+compare receipts, one
pre-existing gap (`business-logic-check` missing from architecture.json) found and fixed in the
same edit, four diagrams left alone with reasons stated, no SKIP, `npm test` green (17 suites,
exit 0) at 1.30.0.
