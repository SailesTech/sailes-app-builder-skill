Result against Done-when: all five commands pass.
- `grep -nE 'merge-base' agents/qa.md codex-agents/qa.toml agents/checker.md codex-agents/checker.toml` → hit in each (qa.md:14, qa.toml:9, checker.md:17, checker.toml:13)
- `grep -n 'comm -23' agents/qa.md agents/checker.md` → hit in both
- `node codex-agents/parity.test.js` → exit 0, "codex parity: all tests passed (10 roles, both sides)", including the new concept "red compared by name against merge-base, never by count" on both `qa` and `checker`
- `node tools/sync-blocks.js --check` → "all blocks in sync"
- `npm test` → exit 0, `grep -c "not ok"` = 0, tail ends "token-report.frozen: all tests passed"

Mutation proof (scratch copy at `/tmp/.../scratchpad/be-dev-P5ab-B2-mutation-check`, `agents/`+`codex-agents/` only, outside repo): untouched copy → exit 0. Deleted the P4 bullet from scratch `agents/checker.md` only → `FAIL checker: "red compared by name against merge-base, never by count" survives in BOTH twins`, exit 1, and that was the *only* failure (qa stayed green). Restored, then deleted the equivalent bullet from scratch `agents/qa.md` only → `FAIL qa: "...` (same label), exit 1, only that failure, checker stayed green. Restored both, re-ran → exit 0. No repo file was ever mutated; scratch dir removed afterward.

Deviations / ambiguities (not decided, flagged):
1. Brief said claim `.claude/status/be-dev-P5ab-B2.md`, but this task is P4, not "P5ab" — followed literally as instructed.
2. `.claude/status/` already held `be-dev-P4-quality-gates.md` from an earlier/parallel run at 14:19 — did not touch it, per instruction not to touch other `be-dev-*` files.
3. Regex for the parity concept uses order-independent lookaheads (`merge-base` + `comm -23` anywhere in the doc) rather than a fixed-distance window, since the two terms sit &gt;200 chars apart in prose — flagging the design choice since the brief only said "the regex must match," not how tight.
4. During the mutation proof, my first scratch directory (a shared-sounding name, `parity-mutation-p4`) was overwritten/emptied mid-run by what appears to be a different, concurrent process writing into the same session scratchpad (files like `p4-diff.txt`, `spec-at-738be36.md` I never created appeared/disappeared there). Re-ran the whole proof under a uniquely-named directory (`be-dev-P5ab-B2-mutation-check`); no repo file was affected. Worth the lead's attention if other P4 workers are running concurrently against the same scratch path.

Blockers: none.

Promotion candidate: none — no runtime code, no test went red on a real defect; `parity.test.js`'s new assertion is TDD-red (concept absent before this commit), not a caught defect.
