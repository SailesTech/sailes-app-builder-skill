# Docs delta — 1.36.0 P6.2 (harness guards from ECC audit: block-no-verify, toolchain-guard)

**Outcome: CONTENT UPDATED, RECEIPT BLOCKED.** `architecture` carries an evidence-backed content
edit for this release; it cannot be handed off with a passing `deliver`/`compare` receipt this
cycle, for the same pre-existing reason recorded at 1.33.0/1.34.0/1.35.0 (not caused by 1.36.0
content). `workflow`, `dataflow`, `sequence`, `lifecycle` carry **no content change** for 1.36.0 —
evidence below is what was checked to reach that conclusion.

## Archify / graphify (verified this session, not taken on trust)

- `~/.claude/skills/archify` reports `"version": "2.17.0-dev.1"` (floor 2.12, `skill-release.json`
  and `package.json` both confirm). `doctor` → 14/14 `[ok]`, exit 0. Not a SKIP — the authoring loop
  ran in full against a live install.
- `graphify` IS installed on this machine: `command -v graphify` → `/home/charlie/.local/bin/graphify`,
  `graphify --version` → `0.9.61`. **`.ai/STATE.md:20` currently states "no `graphify`" — that line
  is stale** (the human flagged this before dispatch and said they were correcting it separately;
  independently reproduced here: the binary is present and answers `--version`). No `graphify-out`
  directory exists in the repo, so there is no fresh map to query for this delta — evidence for the
  content edit below came from reading the changed source files directly (`hooks/hooks.json`,
  `hooks/block-no-verify.js`, `hooks/toolchain-guard.js`, `package.json`, `AGENTS.md`), which is the
  documented fallback order in `skills/sailes-docs/references/authoring.md` ("the graphify map when
  present and fresh, then the code itself").

## Source diff checked

Branch `feat/1.36.0-harness-guards`, HEAD `d0b641c6f26de4e20c30c0211216199b1784e153`. Content read
for this task's named changes: `hooks/hooks.json` (two new `PreToolUse` blocks, matchers `Bash` and
`Edit|Write|MultiEdit`, alongside the pre-existing `Workflow` block), `hooks/block-no-verify.js`
(new, matcher `Bash`, every repo — no `AGENTS.md`/`.ai/` gate, deliberately, per its own header
comment citing spec 1.36.0 P3/Q1), `hooks/toolchain-guard.js` (new, matcher `Edit|Write|MultiEdit`,
Sailes repos only via `isSailesRepo()` — `AGENTS.md` or `.ai/` at repo root — released by
`SAILES_TOOLCHAIN_GUARD=off`, protects `.eslintrc*`/`eslint.config.*`/`.prettierrc*`/
`prettier.config.*`/`biome.json*`/`.ruff.toml`/`ruff.toml`/`tsconfig*.json`, explicitly not
`package.json`); `package.json` `test` script (now runs `hooks/block-no-verify.test.js` and
`hooks/toolchain-guard.test.js`, 25 suites total, up from 23); `AGENTS.md` Verification section
(already carries the 1.36.0 doctrine text describing both hooks and the 25-suite count — read as
confirming evidence, not edited by this role).

## What changed, with evidence (`architecture.json`)

| Diagram | Node/card | Change | Evidence |
|---|---|---|---|
| architecture | "Framework Repo" card, hooks bullet | rewrote to name all three `PreToolUse` hooks instead of one: `workflow-agenttype-guard.js` (unchanged, 1.35.0), plus new `block-no-verify.js` (matcher `Bash`, every repo, no escape hatch — blocks `--no-verify`/`-n`/`core.hooksPath`) and new `toolchain-guard.js` (matcher `Edit\|Write\|MultiEdit`, Sailes repos only, `SAILES_TOOLCHAIN_GUARD=off` release valve); added the shared enforcement mechanism (`stderr` + `process.exit(2)`, never `permissionDecision`) that both new hooks share with the existing one | `hooks/hooks.json` (two new `PreToolUse` entries); `hooks/block-no-verify.js` and `hooks/toolchain-guard.js` header comments (both cite spec 1.36.0 and both state "never `permissionDecision` … would bypass the user's own permission prompt") |

No node, edge, boundary, or view was added or removed — the `hooks` node itself keeps its existing
tag `"SessionStart + PreToolUse"` (accurate before and after: both new hooks are `PreToolUse`, the
category set is unchanged, only the count and identity of hooks inside it changed) and the
`hooks-session-start` edge keeps its label `"SessionStart + PreToolUse"` for the same reason. Only
the card bullet's prose was widened — the same class of edit the 1.30.0/1.33.0/1.34.0/1.35.0
precedent tracks for a hooks/governance-tool capability change.

The `tools` node and its card bullets are **unaffected**: 1.36.0 adds no new `tools/*.js` governance
tool (still 8 in `npm test`), and the two new hook tests fold into the existing hook-test surface at
the level this diagram already tracks — same reasoning as every prior hook-test addition here.

## What was checked and found unchanged (`workflow`, `dataflow`, `sequence`, `lifecycle`)

- `workflow.json` — depicts the client-app build track (sailes-start → discovery → bootstrap →
  spec → pre-implement → implement → phase gates → release gate), not the framework's own
  `PreToolUse` guard hooks. `grep` for `block-no-verify|toolchain-guard|no-verify|SAILES_TOOLCHAIN_GUARD|
  contract-probe|hooks.json` across the file: no matches. No edit made.
- `dataflow.json` — the `npm-test-validators` node's sublabel ("hooks + TOML + frontmatter + release
  hygiene") already covers "hooks" at the category level; it does not enumerate individual hook
  files, so a count going from 23 to 25 suites and two new hook files does not change what this
  sublabel asserts. Same grep across the file: no matches. No edit made.
- `sequence.json` — "Closing a Spec (with the Docs-Delta Gate)" actors/messages are unrelated to
  `Bash`/`Edit`/`Write` guard hooks. No matches. No edit made.
- `lifecycle.json` — spec draft → implemented/archived/superseded states; unaffected by 1.36.0. No
  matches. No edit made. Also the only diagram that still **passes**
  `validate --quality showcase` (0 errors, 0 warnings) on this machine's archify 2.17.0-dev.1.

## Root cause (pre-existing, not 1.36.0) — checked, not assumed

The task brief said the 1.34.0 delta was "blocked by archify 2.17 debt" and asked me to establish
what that debt actually is before treating it as blocking here too. Established: it is
`composition/desktop-readability` (archify's showcase-quality floor, present since 2.12, first
tripped on this repo's diagrams at 1.33.0) rejecting `architecture`, `workflow`, `dataflow` and
`sequence` because their JSON sources carry more node/sublabel text than the rendered viewBox shows
legibly at a 1440px desktop viewport — a **layout/geometry** debt, unrelated to graphify's presence
or absence on this machine. Reproduced independently this session on:
- the **unmodified merge-base** `architecture.json` (`git show d0b641...:docs/architecture/architecture.json`,
  before my edit) → same `composition/desktop-readability` error, same evidence block (`"text":
  "STATE.md, lessons.md, backlog.md, archive/"`, `projectedFontPx: 2.63` vs `minimumProjectedFontPx: 6`).
- `workflow.json`, `dataflow.json`, `sequence.json` on disk, untouched by this delta → each fails
  with pre-existing sublabel-width/`clean-flow` errors, byte-identical diagnostics class to the
  1.34.0/1.35.0 deltas.

This is the same open backlog row (`.ai/backlog.md`: "Re-layout
`docs/architecture/{architecture,dataflow,workflow,sequence}` for archify 2.17's
`composition/desktop-readability` floor … since 1.33.0 … human accepted as recorded debt"). The
re-layout is out of scope for this task, and repeating it here would not repair anything this
delta's content edit introduced — the same failure fires on the unmodified base.

## Commands run, exit codes, real output

- `archify doctor` → 14/14 `[ok]`, exit 0.
- `command -v graphify` → `/home/charlie/.local/bin/graphify`; `graphify --version` → `0.9.61`.
- `archify validate architecture docs/architecture/architecture.json --quality showcase --json` →
  `composition/desktop-readability` error, exit 1. Full JSON:
  `.ai/docs-deltas/2026-09-20-release-1.36.0-architecture-validate.json`.
- Same check against the merge base (`git show d0b641c6...:docs/architecture/architecture.json`,
  unmodified) → identical `composition/desktop-readability` error — confirms pre-existing, not
  introduced by this delta's card-bullet edit.
- `archify deliver architecture docs/architecture/architecture.json
  docs/architecture/architecture.html --quality showcase --json` → `"ok": false`, "Final artifact
  check failed; the previous artifact was preserved." — `architecture.html` on disk is unchanged
  from before this session; `deliver` correctly refused to overwrite it with a failing artifact.
- `archify validate workflow docs/architecture/workflow.json --quality showcase --json` → `"ok":
  false`, pre-existing tag-width + `clean-flow/endpoint-side-direction` errors, no 1.36.0 content
  touches this diagram.
- `archify validate dataflow docs/architecture/dataflow.json --quality showcase --json` → `"ok":
  false`, pre-existing sublabel-width + `clean-flow/endpoint-side-direction` errors, no 1.36.0
  content touches this diagram.
- `archify validate sequence docs/architecture/sequence.json --quality showcase --json` → `"ok":
  false`, pre-existing participant sublabel-width errors, no 1.36.0 content touches this diagram.
- `archify validate lifecycle docs/architecture/lifecycle.json --quality showcase --json` → `"ok":
  true` (9 artifact checks; composition showcase: 0 errors, 0 warnings).
- `archify compare architecture <merge-base architecture.json> docs/architecture/architecture.json
  .ai/docs-deltas/2026-09-20-release-1.36.0.html --receipt .ai/docs-deltas/2026-09-20-release-1.36.0.json
  --json --quality showcase` → `"ok": false`, `"error": "Base snapshot failed validation: Validated
  snapshot failed final artifact checks."`, `composition/desktop-readability` on the **base** side —
  confirms this is pre-existing and not introduced by the hooks-bullet edit above. Full JSON:
  `.ai/docs-deltas/2026-09-20-release-1.36.0-compare-attempt.json`. No receipt file was produced
  (compare never reached the stage that writes one); the `.html`/`.json` positional targets named in
  the command above were never created and are not present on disk.

## What the receipt does not have

- No passing `deliver` receipt (sha256 + bytes) for `architecture.json` this cycle — blocked by the
  pre-existing composition failure above, not by anything this release changed.
- No `compare` delta (added/removed/changed element list) — compare could not get past validating
  the base snapshot.
- `architecture.html`, `workflow.html`, `dataflow.html`, `sequence.html`, `lifecycle.html` on disk
  are **unchanged** — the four unedited diagrams carry an explicit empty delta for 1.36.0, and
  `deliver` correctly refused to overwrite `architecture.html` with a failing artifact.
- `docs/architecture/client-package/*.html` — not refreshed this cycle: the one changed diagram's
  `deliver` was blocked, so there is nothing new to copy in.

## Findings reported upward (not fixed — outside this role's lane)

- `.ai/STATE.md:20` says "This machine has no `graphify`" — false as of this session (`graphify
  0.9.61` present, `command -v graphify` resolves). The human said before dispatch that they were
  correcting this line separately; flagging it here anyway per the "document what IS" discipline, in
  case that correction has not landed by the time this delta is reviewed.
- Doctrine text in `AGENTS.md`'s Verification section already reflects 1.36.0 (both new hooks, the
  25-suite count) at HEAD — no drift found there; not a defect, noted only because the delta task
  asked me to check it.

## Verification

- `git rev-parse --abbrev-ref HEAD` → `feat/1.36.0-harness-guards`; `git rev-parse HEAD` →
  `d0b641c6f26de4e20c30c0211216199b1784e153`.
- `python3 -c "json.load(open('package.json'))['scripts']['test']"` → counted 25
  `node <suite>.test.js` invocations, including `hooks/block-no-verify.test.js` and
  `hooks/toolchain-guard.test.js` (new), up from 23.
- `cat hooks/hooks.json` → three `PreToolUse` entries (`Workflow`, `Bash`, `Edit|Write|MultiEdit`),
  two `SessionStart` entries — confirms the two new hooks are wired, not just present as files.
- Drift check across `workflow.json`, `dataflow.json`, `sequence.json`, `lifecycle.json`: grepped for
  `block-no-verify|toolchain-guard|no-verify|SAILES_TOOLCHAIN_GUARD|hooks.json` — no matches in any
  of the four; each file's own abstraction level (generic phase/actor/state names, not per-hook
  detail) is why the 1.30.0/1.33.0/1.34.0/1.35.0 precedent left them alone for comparable prior
  additions, applied identically here.
