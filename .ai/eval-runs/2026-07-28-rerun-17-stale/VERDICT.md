# Rerun of the 17 STALE scenarios — 2026-07-28, branch feat/archify-docs, pre-merge

Trigger: the archify-gated-docs branch edited files named by 17 pre-existing scenarios
(`eval-status.js` flagged them). Human chose "re-run now" over "merge with debt recorded".

Vehicle for ALL arms: stand-in (`general-purpose` + working-tree text, model set on the
invocation). Grades the TEXT of the doctrine as edited on this branch. 21 arms total across
17 scenarios; every verdict graded from the artifact FILE (or fixture repo state), never
from the agent's summary. Per-scenario detail lives in each scenario's own `Last run:` line
— this file records only the batch shape and what does not fit a line.

## Result: 16 PASS · 1 split (diagnose: treatment PASS a-e, control INCONCLUSIVE)

- The control could not diverge: with the plugin installed machine-wide, skill descriptions
  route BOTH arms to sailes-diagnose. Environment limitation, not a behavior miss; backlogged
  as a fixture-sharpening item. The protected behavior held in both arms.

## Fixture ground truths (kept per the cleanup rule)

- qa-vision: generated PNG pair — baseline button #2563EB (matches the design artifact),
  fresh #DC2626; deviation asserted before dispatch. This also CLOSES the old
  environment-block on this scenario: the vision half needs no browser MCP.
- researcher: riggings verified against the archify clone before dispatch (LICENSE:1 real;
  "18.17" nowhere on disk, real floor >=18 at package.json:12; the exit-code quote lives in
  SKILL.md:35 and in no README).
- diagnose: treatment mandate was generated by the REAL hook (workflow-router.js) against
  the fixture repo, not hand-written.
- scaffolding: lessons.md sha recorded pre-dispatch; byte-identical post-run (shasum -c).

## Findings the batch produced beyond its verdicts (all backlogged 2026-07-28)

1. `graphify-setup.md` portability `sed -i -E` is GNU-only — silent no-op on macOS.
2. `repo-done-checklist.md` hook check hardcodes `.git/hooks/post-commit` — false-negative
   on husky-managed repos.
3. `agents-md-template.md` claims lint-on-raw-literals that bootstrap never ships.
4. Two arms independently verified live Agent-tool constraints (aliases only, no `effort`
   param) — consistent with STATE.md's recorded facts, now re-observed.

## What this batch does NOT cover

Runtime pins, tool allow-lists, and spawn restrictions — stand-ins by definition. The
1.22.0 roster half (docs-author pin/tools) is graded after merge, one real spawn, same as
the 2026-07-26 role-runtime audit.
