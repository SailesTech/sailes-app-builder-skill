# Docs delta — release 1.31.0

## Verified against the repo (not trusted from the brief)

All six claimed changes confirmed present, each read directly from the diff/file rather than the
brief's summary:

1. `skills/sailes-test/SKILL.md` — Step 5 tier table gained a **Case list** column (`git diff`
   confirms the third column and the tier-specific enumeration/partition/one-per-partition rules).
   New `DEAD` disposal route confirmed: "Name it on the detection-proof table as `DEAD`, with the
   mutant that survived it, and carry it to the human as a strike candidate" — a case that survives
   its own mutant, distinct from an equivalent-mutant explanation.
2. `agents/checker.md` + `codex-agents/checker.toml` — confirmed a second mandatory verdict section
   "what the diff contains that the spec does not require," mirroring the existing omission section;
   "scope creep" removed from the prose bullet list in both files. `codex-agents/parity.test.js`
   confirmed to carry a new invariant on `checker`: `['also hunts SURPLUS — what the diff contains
   that the spec does not require', /does not require/i]`.
3. **File move confirmed exactly as described.** `git status` shows `skills/sailes-implement/SKILL.md`
   modified (its ~27-line capability-sweep block replaced by a one-line pointer: "Run the two-grep
   stale-comment sweep before closing — `capability-sweep.md` in this skill") and a new untracked
   `skills/sailes-implement/capability-sweep.md` carrying that content verbatim (opening line: "Cited
   from `sailes-implement/SKILL.md`, 'On completion'. It sits here rather than in the always-loaded
   skill body because it runs once per capability, not once per commit.").
4. `skills/sailes-bootstrap/delegation-threshold.md` — confirmed its sync block shrank: the gate-scaling
   restatement ("Gates scale with what can break… `checker` on any diff… `qa` wherever there is
   behavior to observe…") was replaced by a one-line pointer to the `gate-scaling` block in the same
   file. `tools/blocks.json`-driven consumers restamp via `sync-blocks`; confirmed still **three**
   named blocks system-wide (delegation-threshold, gate-scaling, spec-weight) — no new block added,
   `npm test`'s sync-blocks suite passes.
5. `evals/fixtures/fault-injection/` confirmed new and untracked (`README.md`, `SPEC.md`,
   `mutants.json`, `run-mutants.js`, `validate.js`) — a runnable mutation harness, standalone
   (`node run-mutants.js <suite>`), **not wired into `package.json`'s `test` or `test:all` scripts**.
   New eval scenario confirmed: `evals/tier-scales-the-case-list-not-only-the-proof.md`. (Only one
   new top-level eval file found under `evals/`, not two as the brief characterized item 5 — the
   second "new scenario" the brief may be counting is the fault-injection fixture's own
   `evals/fixtures/fault-injection/SPEC.md`, which is a fixture spec for the harness, not a
   protected-behavior eval scenario in the `evals/*.md` sense used elsewhere. Reported as found,
   not silently reconciled to the brief's count.)
6. `skills/sailes-bootstrap/agents-md-template.md` and `skills/sailes-bootstrap/SKILL.md` — confirmed
   the wording change: "NEVER delete tests" → "NEVER delete a test to reach green" (`SKILL.md` Step 2)
   and "NEVER delete tests or bypass typecheck" → "NEVER delete a test to reach green, and never
   bypass typecheck… (Deleting a case a named replacement made redundant, or one proven to detect
   nothing, is a different act with its own gate — it goes to the human, never quietly.)"
   (`agents-md-template.md`).

## `docs/architecture/architecture.json` accuracy check (per task note)

Confirmed still accurate for 1.31.0 with no edit needed:
- `npm test` runs **17 suites** (counted from `package.json`'s `test` script string). Checked
  against `AGENTS.md` §Verification, which already correctly reads "seventeen suites" and "six
  governance tools" (`AGENTS.md:103`) — that stamp was already current going into 1.31.0 (its own
  `git diff` shows only the `Framework-Version:` header line changed for this release, no text
  drift found). No discrepancy here; retracting an earlier draft of this note that misread the
  count before re-checking `git diff AGENTS.md` directly.
- `tools/*.js` governance-tool count is still **6** (sync-blocks, ownership-check, worker-status,
  mcp-toolnames-check, business-logic-check, deployed-surface-check) — no 7th tool shipped in
  1.31.0. The diagram's card bullet ("six governance tools in npm test, up from five") is unchanged
  and still correct.
- `sync-blocks` still stamps exactly 3 named blocks (delegation-threshold, gate-scaling,
  spec-weight) — the diagram's tag "three blocks: delegation-threshold, gate-scaling, spec-weight
  (1.30.0)" is unchanged and still correct; item 4 above is a content shrink inside an existing
  block, not a new or removed block.
- No new eval-harness component was added that the `evals` node's generic sublabel
  (`evals/*.md + harness/`) does not already cover.

## Judgment on item 3 (the file move) — precedent-based

**No diagram change.** `docs/architecture/architecture.json` draws the entire `skills/` tree as one
component (`{"id":"skills", "label":"Skills", "sublabel":"skills/*/SKILL.md — source of truth"}`) —
individual skills are not nodes, and no skill's internal reference/companion files are drawn either.
Checked the precedent directly: `grep` for `sailes-test`, `sailes-diagnose`, `references/`, or any
per-skill filename across all five diagram JSONs returns **zero matches**. `skills/sailes-test/`
already has a `references/` directory (`techniques.md` et al.) and `skills/sailes-diagnose/` already
carries its own companion files, and neither has ever been drawn as a separate node or edge on any
of the five diagrams. A skill gaining `skills/sailes-implement/capability-sweep.md` — content moved
out of the always-loaded `SKILL.md` body into a cited reference file, same skill, same behavior —
is the same shape at the same granularity the diagrams already choose not to represent. Drawing it
now would be inventing a new level of detail for one skill that the other four/five skills with
reference files don't get, which is inconsistent rather than more accurate.

## Diagrams changed

**None.** All five (`architecture`, `dataflow`, `lifecycle`, `sequence`, `workflow`) validate clean
at showcase quality as they stand — no edit was made to any of them.

```
ok architecture …\docs\architecture\architecture.json (9 artifact checks; composition showcase: 0 errors, 0 warnings)
ok dataflow     …\docs\architecture\dataflow.json     (9 artifact checks; composition showcase: 0 errors, 0 warnings)
ok lifecycle    …\docs\architecture\lifecycle.json    (9 artifact checks; composition showcase: 0 errors, 0 warnings)
ok sequence     …\docs\architecture\sequence.json     (9 artifact checks; composition showcase: 0 errors, 0 warnings)
ok workflow     …\docs\architecture\workflow.json     (9 artifact checks; composition showcase: 0 errors, 0 warnings)
```

This is the **empty-delta** outcome the task brief predicted, and it holds up under verification —
not because I agreed with the prediction going in, but because every one of the six changes turned
out to be either (a) prose/doctrine content inside a node already drawn generically (checker's
verdict sections, sailes-test's tier table, delegation-threshold's block body, the "delete tests"
wording), (b) a within-skill file reorganization the diagrams never represented at that granularity
even before this release (item 3, per the precedent above), or (c) a new standalone eval fixture
that fits inside the `evals` node's existing generic sublabel and is not wired into any gate the
diagrams track (item 5). None of the six introduced, removed, or rewired a *component* or
*connection* as the five diagrams model them.

**Compare receipt produced anyway, as proof rather than assertion:** ran
`archify compare architecture` between the committed `HEAD` copy of `architecture.json`
(`e7124d58e71c3e6830ee8da4bd7b37e6e6507cbb`) and the working-tree copy (unedited by me) —
`.ai/docs-deltas/2026-08-30-release-1.31.0.json` (HTML sibling gitignored per `.gitignore:33`, not
committed). Result: **0 added / 0 changed / 0 removed** components, connections, and boundaries;
`semanticSha256` identical between base and head (`77d80b8d42e4d019b2e63c763a33c344a41d475d72dc957172bb4626872fa9b8`);
28/28 validation checks passed on both compositions. This is the receipt for "no change was needed,"
not for "no change was made" — the two coincide here because I made none.

## Discrepancies found (file:line) — reported upward, not fixed (out of lane)

- Brief's item 5 count: I find **one** new protected-behavior eval scenario
  (`evals/tier-scales-the-case-list-not-only-the-proof.md`) plus the fault-injection fixture
  directory's own internal `SPEC.md` (a fixture spec for the harness, not a second top-level
  `evals/*.md` scenario in the same class). If a second `evals/*.md` scenario exists elsewhere and
  I missed it, it did not surface under `git status --porcelain -uall` at the time of this check.

## What could NOT be established from evidence

- Whether the second "new eval scenario" the brief references (item 5) is a file not yet created,
  a different file I mis-scoped, or a miscount in the brief — could not be resolved from the
  working tree as it stood at check time.
- No graphify map was consulted (none was found fresh for this pass); evidence here is `git diff`/
  `git status` against the actual files, per the discipline's fallback order.

## SKIP

No SKIP fired. Archify **is** installed and above floor: `$ARCHIFY_HOME` resolved to
`C:/Users/karol/.claude/skills/archify`, `SKILL.md` reports `version: "2.12"` (floor is `>= 2.12`),
and `archify doctor` reports all renderers/schemas ready.

## `npm test` verification

Ran `npm test` in the repo root. Exit code **0**. Final suite line:
`release-hygiene: all tests passed (five stamps at 1.31.0)`. No `not ok` / failure lines observed
in full output (checked via grep for failure markers — only substring matches inside passing test
names, e.g. "…FAILS on drift…" as a test title, none of them actual failures). Representative tail
of output:

```
release-hygiene: all tests passed (five stamps at 1.31.0)
  ok   a bare `Status: implemented` is REJECTED — the gate can fail
  ...
spec-status evidence: all tests passed
  ...
repo-done-checklist: all tests passed
  ...
hooks-template: all tests passed
```

## Files touched this pass

- `.ai/docs-deltas/2026-08-30-release-1.31.0-notes.md` — this report.
- `.ai/docs-deltas/2026-08-30-release-1.31.0.json` — compare receipt (base=HEAD, head=working
  tree; 0 changes). Committed artifact per convention.
- `.ai/docs-deltas/2026-08-30-release-1.31.0.html` — compare rendering, gitignored
  (`.gitignore:33`), not for commit.
- `docs/architecture/*.json` / `*.html` — **not touched.** Left exactly as they were; no edit was
  warranted per the findings above.
- `.claude/status/docs-author-2.md` — status claim for this run (see note on numbering below).

## Status-file numbering note

An existing `.claude/status/docs-author-1.md` was found already closed (`outcome: done`) from a
prior 1.30.0 run. No id was assigned to me explicitly for this task, so I claimed the next
unclaimed sequential id, `docs-author-2`, rather than reusing `docs-author-1` (which would have
overwritten that prior worker's closed declaration — exactly the collision the "never one you
choose" rule exists to prevent). Flagging this choice explicitly since it was inferred, not given.
