# Docs delta — 1.35.0 P6.2 (agenttype guard, ownership-check --spec, token-report workflow layout)

**Outcome: CONTENT UPDATED, RECEIPT BLOCKED.** `architecture` carries an evidence-backed content
edit for this release; it cannot be handed off with a passing `deliver`/`compare` receipt this
cycle, for the same pre-existing reason recorded at 1.33.0/1.34.0 (not caused by 1.35.0 content).
`workflow`, `dataflow`, `sequence`, `lifecycle` carry **no content change** for 1.35.0 — evidence
below is what was checked to reach that conclusion.

## Archify

Present at session start: `~/.claude/skills/archify` reports `version: "2.17"` (floor 2.12).
`doctor` → 14/14 `[ok]`. Not a SKIP — the authoring loop ran in full against a live install.

## Source diff checked

Merge `9227eab7a6f95185f6d4928e95ec68ff5ef7defb` (ff-only) confirmed against prior HEAD
`9f79632c071a59d5b4f4555896ff08e057c7c6ec`: 73 files changed. Content read for this task's four
named changes: `hooks/hooks.json` (new `PreToolUse` block, matcher `Workflow`, running
`hooks/workflow-agenttype-guard.js`), `hooks/workflow-agenttype-guard.js` (371 lines) +
`hooks/workflow-agenttype-guard.test.js` (399 cases) — wired into `package.json` `test` script;
`tools/ownership-check.js` lines 190–260ish (`--spec` mode, spec 1.35.0 P2 — second entry point
reading a spec's own `## Fazy` `Owns:` tables and `## Plan wykonania`, alongside the pre-existing
run-log `ownership:`-block mode, unchanged); `tools/token-report.js` (Workflow-dispatch layout
`<session>/subagents/workflows/wf_*/agent-*.jsonl`, and `--cost` USD-estimate flag priced by each
transcript's real `message.model`); `skills/sailes-bootstrap/workflow-orchestration.md` (new
doctrine file, 248 lines).

## What changed, with evidence (`architecture.json`)

| Diagram | Node/card | Change | Evidence |
|---|---|---|---|
| architecture | `hooks` component | tag `SessionStart` → `SessionStart + PreToolUse` | `hooks/hooks.json` now has a `PreToolUse` block (matcher `Workflow`) in addition to the existing `SessionStart` block — the component's own trigger set changed, not just its file list |
| architecture | `hooks-session-start` edge (hooks → claude-session) | label `SessionStart` → `SessionStart + PreToolUse` | same evidence as above; the edge's label is where the diagram states which lifecycle points hooks fire at |
| architecture | "Framework Repo" card, hooks bullet | "Hooks fire at SessionStart; evals gate..." → names `hooks/workflow-agenttype-guard.js` (added 1.35.0, wired in `hooks/hooks.json`), blocks a Workflow dispatch whose declared agentType mismatches | `hooks/workflow-agenttype-guard.js` + `hooks/hooks.json` PreToolUse block above |
| architecture | "Framework Repo" card, new bullet for `tools/ownership-check.js` | added — this tool had no dedicated bullet before 1.35.0 despite being one of the seven governance tools in `npm test`; names the new `--spec` mode as a second entry point | `tools/ownership-check.js:192-197` doc comment: "`--spec` mode (spec 1.35.0 P2) — reads a spec's own `## Fazy` ... instead of a plan's fenced `ownership:` yaml block ... second, additive mode on the same CLI and module" |
| architecture | "Framework Repo" card, token-report bullet | appended clause: now also walks the Workflow-dispatch layout (`subagents/workflows/wf_*/agent-*.jsonl`) and adds `--cost` USD estimates priced by each transcript's real model | `tools/token-report.js` usage line (`--cost` flag), layout comment ("Workflow subagents (P1.1): `<dir>/<session>/subagents/workflows/wf_<id>/agent-<n>.jsonl`"), and the P1.2 real-model pricing comment |

No node, edge, boundary, or view was added or removed beyond the `hooks` node's own tag and its
`hooks-session-start` edge label; the two new card bullets/edits are the same class of edit the
1.30.0/1.33.0/1.34.0 precedent already tracks for a governance-tool capability change.

## What was checked and found unchanged (`workflow`, `dataflow`, `sequence`, `lifecycle`)

- `workflow.json` — depicts the client-app build track (sailes-start → discovery → bootstrap →
  spec → pre-implement → implement → phase gates → release gate), not the framework's own internal
  Workflow-dispatch mechanism. `grep` for `contract-probe|ownership-check|--spec|token-report|
  --cost|workflow-agenttype|PreToolUse|workflow-orchestration` across the file: no matches. The new
  `skills/sailes-bootstrap/workflow-orchestration.md` doctrine and the `workflow-agenttype-guard.js`
  hook are internal dispatch mechanics one level of abstraction below what this diagram tracks
  (generic `phase_gates`/`implement` nodes) — same reasoning the 1.34.0 delta applied to the lane
  split. No edit made.
- `dataflow.json` — the "Primary Path" card's isolation bullet ("Every writer runs in its own
  worktree; it commits there, never to the shared branch, and never pushes — the lead cherry-picks
  finished work in") already generically covers a Workflow-dispatched worker's worktree, and the
  "Two Kinds of Verification" card's `npm test` bullet stays accurate at its category level (does
  not enumerate individual governance tools or hooks). Same `grep` across the file: no matches. No
  edit made.
- `sequence.json` — "Closing a Spec (with the Docs-Delta Gate)" actors/messages are unrelated to
  hook wiring, ownership-check modes, or token-report layout. No matches. No edit made.
- `lifecycle.json` — spec draft → implemented/archived/superseded states; unaffected by 1.35.0. No
  matches. No edit made. Also the only diagram that still **passes**
  `validate --quality showcase` (0 errors, 0 warnings) on this machine's archify 2.17.

## Root cause (pre-existing, not 1.35.0)

`composition/desktop-readability` (archify 2.17 floor) rejects `architecture`, `workflow`,
`dataflow` and `sequence` — reproduced on the merge base (`9227eab`, before this session's edit),
same as the 1.34.0 delta already recorded against `fb69369`. This is the same open backlog row
(`.ai/backlog.md`: "Re-layout `docs/architecture/{architecture,dataflow,workflow,sequence}` for
archify 2.17's `composition/desktop-readability` floor ... since 1.33.0 the JSON sources carry
content the rendered HTML pages do not show ... human accepted as recorded debt"). The re-layout is
out of scope for this task, and repeating it here would not repair anything my content edit
introduced — the same failure fires on the unmodified base.

## Commands run, exit codes, real output

- `archify doctor` → 14/14 `[ok]`, exit 0.
- `archify validate architecture docs/architecture/architecture.json --quality showcase --json` →
  `composition/desktop-readability` error, exit 1. Full JSON:
  `.ai/docs-deltas/2026-09-17-release-1.35.0-architecture-validate.json`.
- Same check against the merge base (`git show 9227eab:docs/architecture/architecture.json`,
  unmodified) → identical `composition/desktop-readability` error, exit 1 — confirms pre-existing.
- `archify deliver architecture docs/architecture/architecture.json
  docs/architecture/architecture.html --quality showcase --json` → "Final artifact check failed;
  the previous artifact was preserved.", exit 1 (`architecture.html` on disk is byte-identical to
  before this session — `deliver` correctly refused to overwrite it with a failing artifact).
- `archify validate workflow docs/architecture/workflow.json --quality showcase --json` → exit 1,
  pre-existing layout errors, no 1.35.0 content touches this diagram.
- `archify validate dataflow docs/architecture/dataflow.json --quality showcase --json` → exit 1,
  pre-existing layout errors, no 1.35.0 content touches this diagram.
- `archify validate sequence docs/architecture/sequence.json --quality showcase --json` → exit 1,
  pre-existing layout errors, no 1.35.0 content touches this diagram.
- `archify validate lifecycle docs/architecture/lifecycle.json --quality showcase --json` → exit 0
  (9 artifact checks; composition showcase: 0 errors, 0 warnings).
- `archify compare architecture .ai/docs-deltas/.arch-base.json
  docs/architecture/architecture.json .ai/docs-deltas/2026-09-17-release-1.35.0.html --receipt
  .ai/docs-deltas/2026-09-17-release-1.35.0.json --json` → `"stage": "check"`, `"error": "Base
  snapshot failed validation: Validated snapshot failed final artifact checks."`,
  `composition/desktop-readability` on the base side, exit 1 — confirms this is pre-existing and
  not introduced by the two card-bullet/tag edits above. Full JSON:
  `.ai/docs-deltas/2026-09-17-release-1.35.0-compare-attempt.json`. No receipt file was produced
  (compare never reached the stage that writes one); the `.html`/`.json` positional targets named
  in the command above were never created and are not present on disk.

## What the receipt does not have

- No passing `deliver` receipt (sha256 + bytes) for `architecture.json` this cycle — blocked by the
  pre-existing composition failure above, not by anything this release changed.
- No `compare` delta (added/removed/changed element list) — compare could not get past validating
  the base snapshot.
- `architecture.html`, `workflow.html`, `dataflow.html`, `sequence.html`, `lifecycle.html` on disk
  are **unchanged** — the four unedited diagrams carry an explicit empty delta for 1.35.0, and
  `deliver` correctly refused to overwrite `architecture.html` with a failing artifact.
- `docs/architecture/client-package/*.html` — not refreshed this cycle: the one changed diagram's
  `deliver` was blocked, so there is nothing new to copy in, and copying the unchanged HTML over
  itself for the other four would be a no-op recorded as if it were a step.

## Verification

- `git merge --ff-only 9227eab7a6f95185f6d4928e95ec68ff5ef7defb` → fast-forward from
  `9f79632c071a59d5b4f4555896ff08e057c7c6ec`, 73 files changed.
- `node -p` version probe against `~/.claude/skills/archify/SKILL.md` → `"2.17"`, above the 2.12
  floor from `skills/sailes-docs/references/archify-setup.md`.
- Drift check across `workflow.json`, `dataflow.json`, `sequence.json`, `lifecycle.json`: grepped
  for `contract-probe|ownership-check|--spec|token-report|--cost|workflow-agenttype|PreToolUse|
  workflow-orchestration` — no matches in any of the four; each file's own abstraction level
  (generic phase/actor/state names, not per-tool/per-hook/per-mechanic detail) is why the
  1.30.0/1.33.0/1.34.0 precedent left them alone for comparable prior additions, applied identically
  here.
