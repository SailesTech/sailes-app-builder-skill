# checker verdict — P4 "zastana czerwień ustalana na bazie"
Spec: `.ai/specs/2026-09-13-quality-gates-from-the-partner-portal-report.md` (as of `738be36`), P4 section, decisions Q7/R5/F3, and the two binding 2026-09-13 refinements D-P4a/D-P4b.
Diff range reviewed: `738be36..548d3d8` (excluding `.ai/runs`), in worktree `agent-a8f22b0c9cbd4cf4a`.

## Done-when commands (all pass)

```
$ grep -nE 'merge-base' agents/qa.md codex-agents/qa.toml agents/checker.md codex-agents/checker.toml
→ hit in all four files (confirmed)

$ grep -n 'comm -23' agents/qa.md agents/checker.md
→ hit in both

$ node codex-agents/parity.test.js
→ exit 0, includes:
  ok   checker: "red compared by name against merge-base" survives in BOTH twins
  ok   qa: "red compared by name against merge-base" survives in BOTH twins
  codex parity: all tests passed (10 roles, both sides)
```

`node tools/sync-blocks.js --check` also run for completeness (not requested but cheap): `sync-blocks: all blocks in sync`.

## What the diff does NOT do that the spec/decisions require

Looked at the full P4 file table (`skills/sailes-implement/SKILL.md`, `skills/sailes-bootstrap/release-checklist.md`, `agents/qa.md`+`.toml`, `agents/checker.md`+`.toml`, `evals/gate-compares-red-by-name-not-count.md`) against P4.1–P4.4 and D-P4a/D-P4b. All five files are touched, all four Done-when commands pass, and the substance holds up:

- **Known-red entry form** `<file/test name> · <cause> · validity: this push` appears verbatim in `agents/qa.md`, `codex-agents/qa.toml`, and `skills/sailes-implement/SKILL.md`'s run-log template; `release-checklist.md` and the eval file state the "by name, never by count" rule in the same words. A literal count is never offered as an acceptable form anywhere it's mentioned. Nothing missing here.
- **qa's P4.2 procedure** has all four steps in order (paste branch-red sorted → fetch + run same names at merge-base in a detached worktree, paste sorted → paste `comm -23 <branch> <base>`, non-empty = CHANGES-REQUIRED → record base-confirmed names to `Known-red:`). Direction is branch-minus-base, matching F3 ("czerwone na gałęzi minus czerwone na bazie"). Environment exclusivity ("You hold the environment, exclusively" section) and the P3 rules (integrity probe in both lanes, `ENV-DEFECT` on missing chrome-devtools MCP) are untouched by the diff and still present verbatim.
- **checker's P4.3 procedure matches D-P4a/D-P4b exactly**, including the important negative: checker.md/`.toml` explicitly state its base is the fixed left side of the diff range the lead handed it, and explicitly *not* the live `git merge-base HEAD origin/<base>` recomputation qa runs — with the stated reason ("that would let an earlier, unpushed phase's red read as new at every later phase gate"). The `.git worktree add --detach`/`remove` exception is bounded ("for the base run only", "always, including on failure", touches only `.git` worktree metadata) and appears identically in both `agents/checker.md` and `codex-agents/checker.toml`.
- **A name absent from the base run counts as not-red-on-the-base (a new red)** is stated explicitly in both `agents/qa.md` ("a name absent from the base run counts as not-red-on-the-base, so its red on the branch is new") and `agents/checker.md`/`checker.toml` ("A name absent from the base run is not red on the base"). The doctrine does not leave this open.
- **Eval vs P4.4**: the fixture is 3 base-reds (`X1,X2,X3`), this run's branch-red is `{X2,X3,Y}` (X1 fixed, Y new) — same count (3) as the prior push's `Known-red`, so a naive count-comparison arm (A) would wave it through. Arm B runs the base, gets `comm -23` = `{Y}`, reports `Y` as new/CHANGES-REQUIRED, and only `X2`/`X3` land in `Known-red:`. This maps onto P4.4's "3 red on base + 1 new red, same count because one base red was fixed" almost verbatim, and PASS/FAIL are stated as the binary the spec asks for.

Two things I'd want the lead/human to look at even though neither blocks this gate on the letter of P4's Done-when:

1. **The D-P4b extension to `qa`'s pre-push base run is stated consistently (`agents/qa.md` and `codex-agents/qa.toml` use the same "temporary detached worktree outside the repo … touches only `.git` worktree metadata" language, and it is not contradicted by anything already in `agents/qa.md`), but it leaves an open question the diff never resolves: `qa`'s full-suite scope explicitly includes e2e**, which needs a *live* stack, not just a checked-out worktree. If a red name being re-run at merge-base is an e2e case, "run there" implies bringing up a second instance of the app against the shared, singular database/containers — which sits uneasily next to the very next section of the same file, "You hold the environment, exclusively" / "This is the one resource that cannot be cloned." The diff doesn't say whether the base-comparison run is meant to be sequential-with-the-same-environment, scoped to non-live tests only, or something else. Not a spec violation (the human hasn't confirmed this extension yet, per the task's framing, and F3/Q7's original text only ever says "run the same names at merge-base" without specifying mechanism), but it's the kind of gap that will surface the first time a red e2e name needs re-running.
2. Minor: `evals/gate-compares-red-by-name-not-count.md`'s "Skill under test" line credits `agents/checker.md`'s phase-gate mechanism as covered by "the same … concept" as `qa`'s. Read narrowly this is accurate — `parity.test.js` (confirmed above) checks the concept's presence in *both* role definitions, and this file's live A/B narrative only actually role-plays `qa`'s pre-push flow, never checker's fixed-diff-range determination (the specific thing D-P4a exists to distinguish). Worth a one-line clarification in the eval header that the live scenario is qa-only and checker's coverage is via `parity.test.js`'s static check, so a future reader doesn't assume this eval behaviorally proved checker's distinct (non-merge-base) rule too. Cosmetic, not blocking.

## What the diff contains that the spec/decisions do not require

- Nothing invented beyond P4's scope: no new tool, no new role, no new field beyond `Known-red:` (which R5/F3 explicitly call for). The eval's A/B/PASS/FAIL shape matches the format already used by sibling evals in this repo (`lead-picks-the-lane-from-the-tier.md`, etc.), so it isn't new ceremony.
- The only thing beyond the letter of the confirmed decisions is the qa-side extension already discussed above (item 1) — D-P4a/D-P4b as handed to me were framed around checker's need for an exception to read-only; applying the same detached-worktree mechanism to `qa` (who already has unrestricted `Bash` and needs no such exception) is a reasonable but not-yet-human-confirmed engineering choice. It is at least applied uniformly (`.md` and `.toml` agree) rather than inconsistently, so if the human declines it later, there's one place-per-role to revert.

## Regex robustness (report only, not fixed)

Tried to break `/by name[\s\S]{0,400}merge-base|merge-base[\s\S]{0,400}by name/i` used for the `'red compared by name against merge-base'` concept, on scratch strings only (never touched repo files):

- **False negative** — a semantically faithful rewrite that avoids the literal phrases fails the regex: `"Establish pre-existing red using names, never using a count: run the same failing test identifiers against the common ancestor commit shared with origin, then diff the two sorted lists."` → `false`.
- **False positive** — text that name-drops both terms without stating the rule passes: `"Sort the red test list by name for readability. Note: this has nothing to do with the merge-base calculation used elsewhere in the pipeline, which is a separate, unrelated step."` → `true`.

This is the same proximity-regex idiom already used for every other concept in `INVARIANTS` in this file (e.g. the UI-integrity-both-lanes check), so it's an existing, accepted risk pattern in this repo rather than something newly introduced by this diff — flagging per the review checklist's instruction to report, not fix.

## Verdict: NITS

The mechanics are correct and internally consistent: all four Done-when commands pass, `Known-red:` form is uniform everywhere it's stated, qa's four-step procedure is in order with the right `comm -23` direction, checker's procedure correctly diverges from qa's per D-P4a (fixed diff-range base, not a live merge-base recompute), the read-only exception is bounded in both `.md`/`.toml` twins, "absent-on-base = not-red-on-base" is stated explicitly on both sides, and the eval matches P4.4's scenario (including the PASS/FAIL binary) closely.

Non-blocking notes for the lead:
1. Before confirming the D-P4b-to-qa extension, resolve how a red e2e name's base re-run coexists with qa's own "environment cannot be cloned" rule in the same file — the diff is silent on this.
2. Consider one clarifying sentence in `evals/gate-compares-red-by-name-not-count.md`'s header noting the live A/B narrative exercises `qa` only, with checker's coverage coming from `parity.test.js`'s static concept check, so nobody later assumes this eval behaviorally proved checker's distinct base rule.
3. Pre-existing, repo-wide regex fragility in `codex-agents/parity.test.js`'s proximity-match idiom (both false-negative and false-positive constructible) — not new to this diff, reported for awareness.
