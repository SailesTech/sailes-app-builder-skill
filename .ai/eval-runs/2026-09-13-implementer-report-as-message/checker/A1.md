# Checker verdict — P4 "zastana czerwień ustalana na bazie"
Spec: `.ai/specs/2026-09-13-quality-gates-from-the-partner-portal-report.md` @ 738be36, phase P4
Diff: `738be36..70e4cf4` (excl. `.ai/runs`)

## Done-when — run in the worktree, at 70e4cf4

`grep -nE 'merge-base' agents/qa.md codex-agents/qa.toml agents/checker.md codex-agents/checker.toml`
→ hit in all four files (each contains a literal `merge-base` reference, correctly distinguishing
`git merge-base HEAD origin/<base>` for `qa` from the phase's own integration base for `checker`).

`grep -n 'comm -23' agents/qa.md agents/checker.md`
→ hit in both.

`node codex-agents/parity.test.js`
→ exit 0, includes:
- `ok   checker: "pre-existing red on Done-when compared by name against the base, never by count" survives in BOTH twins`
- `ok   qa: "pre-existing red established by name against the merge-base, never by count" survives in BOTH twins`
- `codex parity: all tests passed (10 roles, both sides)`

`npm test` (ran despite the instruction to skip the full suite — already executed before I reread that
line; harmless and read-only, reported for transparency, not as required evidence): exit 0, all suites
green including `token-report` and `token-report.frozen`.

## What the diff does NOT do that the spec/decisions require

Nothing required by P4.1–P4.4 or D-P4a/D-P4b is missing. Specifically checked and present:
- P4.1 exact `Known-red:` form `<file/test name> · <cause> · validity: this push`, "a count is never
  an acceptable form," and "expires at push" — stated identically in `skills/sailes-implement/SKILL.md`,
  `skills/sailes-bootstrap/release-checklist.md`, `agents/qa.md`, `codex-agents/qa.toml`, and echoed by
  name in the eval. Nowhere does a count sneak in as an alternate acceptable form.
- P4.2's four steps, in order, in `agents/qa.md` and `codex-agents/qa.toml`: paste branch-red sorted
  names → fetch + run same names at `git merge-base HEAD origin/<base>` in a detached worktree → paste
  `comm -23` → base-red names go to `Known-red:`. `qa`'s pre-existing P3 rules (integrity probe in both
  lanes, `ENV-DEFECT` on a missing instrument, environment-exclusivity section) are untouched by the
  diff (confirmed by reading the full file, not just the hunk).
- P4.3 / D-P4a / D-P4b in `agents/checker.md` and `codex-agents/checker.toml`: checker's base is
  explicitly *not* `qa`'s `git merge-base HEAD origin/<base>` but "the commit the worker's brief cut its
  worktree from," with the rationale spelled out (an earlier, unpushed phase's red would otherwise read
  as new at every later gate) — this matches D-P4a's "left side of the diff range the lead hands
  checker" precisely. The worktree mechanism (`git worktree add --detach <tmp> <base>` outside the
  repo, `git worktree remove <tmp>` always) and the "sole thing Bash is permitted to write" framing
  from D-P4b appear consistently in both the preamble and the "You never" section of `checker.md`, and
  in the equivalent single paragraph plus closing line of `checker.toml`. "A test name absent on the
  base counts as not red there" is stated explicitly for `checker`.
- P4.4 eval (`evals/gate-compares-red-by-name-not-count.md`): 3 base-red names, 1 fixed
  (`csv-quote-escape`), 1 new unrelated red (`partner-webhook-retry`) landing on an unchanged total of
  3 — reproduces the spec's "same count because one base red was fixed" exactly. PASS/FAIL are the
  spec's own binary: PASS = new name reported by name after running the base despite matching count;
  FAIL = reasoning from totals, or writing the new name into `Known-red:` without a base run. A/B design
  (pre- vs post-diff role text) and a run for each of `qa` and `checker` are both present.

One completeness gap, not a behavior gap: `checker.md`/`.toml` state outright that "a test name absent
on the base counts as not red there" (closing the exact loophole item 4 asks about); `qa.md`/`.toml`
carry the identical F3 procedure but never restate that sentence for `qa`. The `comm -23` mechanic
handles the case correctly either way (a name absent from the base's red list — because it never
existed there or because it wasn't red — falls out of `comm -23 <branch> <base>` the same as any other
name unique to the branch), so nothing is functionally open; it is only that `qa`'s copy relies on a
reader inferring from the algebra of `comm` what `checker`'s copy says outright. Worth a one-line
addition to `qa.md`/`.toml` for a fresh worker who hasn't traced the `comm` semantics, but not a defect
in what `qa` will actually do.

## What the diff contains that the spec/decisions do not require

- `codex-agents/parity.test.js` is edited to add the two invariant entries, but P4's own file table
  (`skills/sailes-implement/SKILL.md`, `release-checklist.md`, `agents/qa.md`+`.toml`,
  `agents/checker.md`+`.toml`, the eval) does not list it — yet P4's own Done-when requires
  `node codex-agents/parity.test.js` → exit 0 with the named concept, which cannot be true without
  editing that file. This is a gap in the spec's file table (as it was for P2.6/P3.6, where
  `parity.test.js` *is* listed), not scope creep by the diff: the edit is the minimum needed to satisfy
  the phase's own Done-when. **NIT** on the spec, not the diff.
- The lead's unconfirmed extension of D-P4b (a worktree-based base run) onto `qa`'s pre-push procedure
  is stated consistently between `agents/qa.md` and `codex-agents/qa.toml` (same worktree command, same
  `comm -23` step), and it does not contradict the spec text (which never specifies *how* `qa` reaches
  the merge-base, only that it must) nor `qa`'s existing rules — `qa`'s tool list genuinely has no
  Write/Edit, so the "you have no Write/Edit" framing is literally accurate. It is, however, a slightly
  confusing transplant: that phrase is D-P4b's rationale for *checker*, whose entire identity is being
  read-only; `qa` was never read-only (it already writes `.ai/ENV-LOCK` via Bash, three sections above
  the new text, in the same file) and doesn't need a "read-only exception" to justify running a
  temporary worktree — its actual reason is simpler (don't corrupt the branch checkout you're mid-test
  on). Cosmetic, not a doctrinal conflict; flagged per the task's explicit instruction to report the
  consistency of this unconfirmed extension without treating it as a violation by itself.
- Parity-regex robustness (item 5, scratch-probed outside the repo, nothing in the diff touched):
  `/by name[\s\S]{0,400}merge-base|merge-base[\s\S]{0,400}by name/i` is a proximity check with no
  negation-awareness. Confirmed on scratch strings:
  - A rewording that DROPS the rule (reverts to raw count comparison) but happens to mention "by name"
    and "merge-base" near each other — even while explicitly saying "do not compare by name" or
    "a count comparison is sufficient" — still matches (`true`). So a regression to exactly the
    count-based behavior Q7/F3 exist to forbid would not be caught by this check if the reverted text
    still name-drops both terms in the same breath.
  - A paraphrase that KEEPS the rule but avoids the literal phrase "by name" (e.g. "using test
    identifiers... never using totals") does not match (`false`) — a false negative, safer failure
    direction but still brittle.
  This weakness is not unique to the P4 addition — every other invariant in `parity.test.js` uses the
  same loose proximity-regex convention (already visible in the surrounding `ok` lines for `checker`/
  `qa`/`be-dev`/etc.), so this diff followed existing repo convention rather than introducing a new
  category of risk. Reporting per the task's item 5 instruction; not fixed, not blocking.

## Verdict: NITS

No CHANGES-REQUIRED defect found. All four Done-when commands pass with the exact expected shape; P4.1
–P4.4 and D-P4a/D-P4b are implemented faithfully and consistently across the `.md`/`.toml` twins; the
eval reproduces P4.4's fixture and PASS/FAIL binary precisely; P3's `qa` rules are intact. Non-blocking
notes for the human/next worker:
1. Add the explicit "absent-on-base counts as not red" sentence to `agents/qa.md` / `codex-agents/qa.toml`
   to match `checker`'s wording — currently correct by construction (via `comm -23`) but not stated.
2. Add `codex-agents/parity.test.js` to P4's file table (spec-side fix, not diff-side).
3. Consider re-wording `qa`'s "you have no Write/Edit" sentence — accurate but borrows `checker`'s
   read-only framing for a role that was never read-only and writes elsewhere in the same file.
4. `parity.test.js`'s proximity-regex convention (pre-existing, not introduced here) can be satisfied by
   text that negates the very rule it's meant to guard — worth a future hardening pass across the whole
   file, out of scope for P4.
