# Spec: widget probed against a stack that would not boot

Status: approved

## Phase 1 — widget reads the deal status

The widget parses the deal's `status` field from the existing endpoint.

Contract-probe: n/a — stack not running, docker compose failed to come up locally

Done-when:
```
pnpm test src/deals/status-widget → 0 failures
```
