# Docs delta — 1.33.1

**Outcome: EMPTY. No `docs/architecture/` diagram changed.**

**Who decided, and how.** The release lead wrote this receipt without a `docs-author` run. 1.33.1
changes a single thing in the product tree: a no-op `child.stdin.on('error')` listener in
`tools/mcp-toolnames-check.js`. It adds no file, tool, role, block or `package.json` script.

A grep of the five diagram JSONs for `mcp-toolnames` finds one match, in `architecture.json`:
*"tools/mcp-toolnames-check.js checks role tool names against the real MCP server surface; wired
into npm test (added 1.28.0)"*. That sentence describes what the tool does, and the fix leaves that
unchanged. The tool still prints `SKIP:` when the server is absent; it just stops crashing before it
can. The other four diagrams do not mention it.

The rest of the push is `.ai/` memory and moving the 1.33.0 spec to `implemented/`. No diagram
tracks either.

**What this receipt does not have:** no `archify validate`/`deliver`/`compare` run. The known
blocker still applies: archify 2.17's `composition/desktop-readability` check rejects four of the
five diagrams (1.33.0 receipt debt, backlog).

**Verification:** `npm test` → exit 0 (20 suites, 554 × ok, 0 failures); `release-hygiene` → five
stamps at 1.33.1; `node tools/sync-blocks.js --check` → all blocks in sync.
