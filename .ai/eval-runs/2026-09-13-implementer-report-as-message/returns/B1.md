Working tree is clean; the status file was untracked and never staged, so nothing else to clean up.

---

**Result against Done-when:** All five conditions met — `grep -nE 'merge-base'` hits in all 4 files; `grep 'comm -23'` hits in both `qa.md`/`checker.md`; `node codex-agents/parity.test.js` → exit 0, 10 roles including the new concept for `qa`/`checker`; `node tools/sync-blocks.js --check` → in sync; `npm test` → exit 0, 0 `not ok` lines.

**Commands run (key ones):**
- `git merge --ff-only 738be36` → fast-forward, HEAD confirmed at `738be36`.
- `grep -nE 'merge-base' agents/qa.md codex-agents/qa.toml agents/checker.md codex-agents/checker.toml` → 4/4 hits.
- `grep -n 'comm -23' agents/qa.md agents/checker.md` → 2/2 hits.
- `node codex-agents/parity.test.js` → `codex parity: all tests passed (10 roles, both sides)`, exit 0.
- Mutation proof on a scratch copy (`agents/`+`codex-agents/` copied to `/tmp/.../scratchpad/p4-mutation`, outside the repo): untouched copy → exit 0; stripping `"by name, never by count"` from `agents/qa.md` → `FAIL qa: "red compared by name against merge-base, never by count"`, exit 1 (only that concept failed); restored a fresh copy, stripped the same phrase from `codex-agents/checker.toml` → `FAIL checker: "..."`, exit 1. Scratch dir deleted afterward; repo files were never touched.
- `node tools/sync-blocks.js --check` → `all blocks in sync`.
- `npm test` → exit 0, `grep -c "^not ok"` → 0.

**Deviations / ambiguities (flagging, not deciding):**
- The spec's grep for `merge-base` requires a hit in `checker.md`/`checker.toml` even though D-P4a's checker base is explicitly *not* `git merge-base` — I satisfied the literal grep by writing a contrast sentence ("this is not qa's `git merge-base HEAD origin/&lt;base&gt;`..."). This is textually correct but is a grep-driven wording choice rather than an obviously-intended one; worth a second look.
- P4.4's eval fixture is my own construction (spec gives only the shape: 3 base-red + 1 new + count parity because one base-red was fixed) — I designed the concrete names/scenario (`test_a/b/c/d`) to match `lead-picks-the-lane-from-the-tier.md`'s format; no existing fixture to copy verbatim.
- Left the `Known-red:` entry-form text (`&lt;file/test name&gt; · &lt;cause&gt; · validity: this push`) only in `SKILL.md`'s Pre-flight step 3, not restated in `qa.md`/`checker.md`, to avoid restating context across files (per AGENTS.md style rule) — both point back to `SKILL.md`/note the run log by name.

**Blockers:** none.

**Promotion candidate:** none — nothing here went red on a real defect; this phase was doctrine-only.

Commit `964f777` on worktree branch `worktree-agent-ac995251ce806eb7c`; status file closed at `.claude/status/be-dev-P5ab-B1.md` (outcome: done).
