# Docs delta — 1.32.0

**Outcome: EMPTY. No `docs/architecture/` diagram changed.**

**Who decided, and how — stated plainly because it matters.** This receipt was written by the
release lead, not by a `docs-author` run. `docs-author` was spawned for 1.31.0 earlier today,
against a change of the same class, and established the precedent by evidence rather than by
principle: it grepped all five diagram JSONs for any per-skill filename, `references/` path,
`sailes-test` or `sailes-diagnose` companion file and found **zero matches**, concluding that the
diagrams draw `skills/`, `agents/` and `codex-agents/` as single generic components and have never
represented per-role sections, per-skill files, or block bodies.

1.32.0 is strictly inside that finding: it edits prose in `skills/sailes-implement/SKILL.md`,
`skills/sailes-test/SKILL.md`, `skills/sailes-test/test-plan-template.md`, `agents/be-dev.md`,
`agents/fe-dev.md` and `agents/tester.md`, adds one eval scenario and one eval-run record, and
creates **no new file under `skills/` or `agents/`**, no tool, no sync block, no role, and no
`package.json` script. Nothing any diagram tracks moved.

**What this receipt does not have**, and a reader should hold it to a lower standard because of it:
no `archify validate`/`deliver` run, and no `archify compare` receipt of its own. 1.31.0's compare
receipt (`2026-08-30-release-1.31.0.json`, 0 added/changed/removed, identical `semanticSha256`) is
the evidence being leaned on, and it describes the tree as of that release plus this one's prose
edits. If that lean is wrong, the way to find out is to run `docs-author` on the next release and
see whether it reports drift it should have caught here.

**Verification:** `npm test` → exit 0 (17 suites); `release-hygiene` → five stamps at 1.32.0;
`node tools/sync-blocks.js --check` → all blocks in sync.
