Status file closed correctly.

**Result against Done-when:** all five pass — see commands/output below. P4.1–P4.4 all implemented; commit `548d3d8` (declaration of done) in worktree `agent-a8f22b0c9cbd4cf4a`.

**Commands run:**
- `grep -nE 'merge-base' agents/qa.md codex-agents/qa.toml agents/checker.md codex-agents/checker.toml` → 1 hit each (4/4)
- `grep -n 'comm -23' agents/qa.md agents/checker.md` → 1 hit each (2/2)
- `node codex-agents/parity.test.js` → exit 0, includes `ok qa: "red compared by name against merge-base"` and `ok checker: "red compared by name against merge-base"`
- Mutation proofs (scratch copies under `/tmp/.../scratchpad`, outside repo, deleted after): stripping the concept from `agents/qa.md` alone → exit 1, `FAIL qa: "red compared by name against merge-base" ... survives in BOTH twins` (checker stayed `ok`). Stripping from `agents/checker.md` alone → exit 1, `FAIL checker: "..." survives in BOTH twins` (qa stayed `ok`). Untouched copy → exit 0. Never touched repo files or used `git checkout` for this.
- `node tools/sync-blocks.js --check` → `sync-blocks: all blocks in sync`
- `npm test` → exit 0, tail ends `token-report.frozen: all tests passed`

**Deviations (ambiguities left for the lead, not decided by me):**
- The Done-when's `grep -nE 'merge-base'` requires a hit in `agents/checker.md`/`codex-agents/checker.toml` too, even though D-P4a says checker's base is the phase's cut base, not a `merge-base` *computation*. I satisfied the grep by having checker's doctrine explicitly name-and-reject the `git merge-base HEAD origin/&lt;base&gt;` mechanism as "not what checker uses" (contrast, not adoption). This reads correct to me but is a literal-grep-driven wording choice worth a second look.
- P4.4's eval fixture is my construction from the spec's one-sentence description (3 base-red + 1 new + equal count via one base-red fixed); I picked `qa` pre-push as the primary scenario and cross-referenced `checker`'s phase-gate variant only in the Files line, not as a second full scenario — flag if a separate checker-specific eval was wanted.
- `skills/sailes-implement/SKILL.md` Pre-flight step 3: I added the `Known-red:` section description inline in the run-log bullet rather than as a separate numbered sub-step; the brief didn't specify placement precision.

**Blockers:** none.

**Promotion candidate:** none — this phase produced no runtime code and no inner-loop check ran against a real defect.
