# Backlog — deferred ideas, later phases, tech debt (framework repo)

> Where non-goals and "not now, but important" land so they survive. Triage periodically;
> promote an item to a spec when it's time.

## Features / ideas (deferred)
| Item | Why deferred | Source (brief/spec) | Status |
|---|---|---|---|
| `deployed-surface-check.js` as a client-repo copy (byte-parity pair in `hooks-template/`, so a client hook can run it) | one parity pair (`business-logic-check`) is the total measured experience with that pattern; doubling it before it has paid twice is speculation. Today the rule reaches client repos as prose in `sailes-spec` / `spec-writing-template.md` | 2026-08-30 spec, Q2 | **next** |
| Re-run `evals/diagnose-runs-live-case-before-audit` against criterion (f) | criterion (f) was added 2026-08-30 in the same change that edited `sailes-diagnose/SKILL.md`, so the eval reads STALE and its recorded PASS does not cover the new criterion. Owed, and the board says so | 2026-08-30 spec, Phase 3 | **next** |
| Sweep the remaining restatements of the role order for a `Lane:` pointer | 1.34.0 P3 put the `full`/`middle` rule in the `gate-scaling` block and pointed four restatements at it, plus `agentic-first-principles.md` at the gate. Still unpointed (grep of `explorer → designer` outside `.ai/`, 2026-09-13): `sailes-discovery/brief-template.md:114`, `sailes-discovery/checklists.md:33`, `agent-team-structure.md` teams-off fallback (~:809), `docs/agent-roles.md:30`, `README.md:159`. The first grep missed the last two, and a second `checker` found them, so re-grep before sweeping. The two `sailes-discovery` lines also **omit `tester`** entirely. That drift is older, and it is a missing gate inside a list that reads as complete | 2026-09-13 P3 checker finding (b), human chose "pointer now, rest to backlog" | **next** |
| ~~Dispatch `evals/mock-of-an-external-boundary-carries-a-pair`~~ | **DONE 2026-09-13** — first run, PASS: the plan declares one deployed `curl` as the pair of the mocked CDN boundary and pairs no internal double. `.ai/eval-runs/2026-09-13-p6-evals/VERDICT.md` | 2026-08-30 spec, Phase 2; run in 1.34.0 P6 | done |
| ~~A/B the `spec-weight` block on a WHOLE-spec prompt~~ | **DONE 2026-08-30** — `.ai/eval-runs/2026-08-30-spec-weight/`, eval `spec-weight-shrinks-a-contract-fix`. 15,102 → 6,719 bytes, 13 sections → 5, every dropped section disposed of with a stated `n/a`. Probe dimension tied on that fixture, recorded as such | 2026-08-30 eval-run VERDICT | done |
| Re-ground `hooks-template/brief-closure.js:36-38` | its comment justifies excluding `Report:` from the Done-when pool with "`Report` describes what the worker writes AFTER the fact" — 1.30.0 retires that premise. The exclusion is probably still right for its second reason; the argument is now wrong | 2026-08-30 worker flag | **next** |
| Re-run volume A/B fixture 1 under the shipped straddle-pair wording | fixture 1 (`normalizeOwner`, no cross-product) returned NULL and its arm B was written under the superseded "walked in full" wording. The NULL is a fixture critique, not a data point for what shipped | 2026-08-30 eval-run VERDICT | **next** |
| Widen `evals/fixtures/fault-injection/mutants.json` beyond spec-derived faults | every mutant today breaks a numbered spec clause, so a race, an encoding or a nullable vendor field is unmeasured — and the 16 cases tier B trims could plausibly cost exactly those. The harness states this limit; closing it needs a different mutant class | 2026-08-30 eval-run VERDICT | **next** |
| Decide whether Red Flags and `Never` in `sailes-test/SKILL.md` should stay ~60% overlapped | three near-verbatim pairs (manual step, mocking your own app, unpaired external boundary); cutting them saves ~320 bytes. Left deliberately: `Never` states rules, Red Flags states symptoms — two instruments, and it is a doctrine call rather than a compression one | 2026-08-30 worker fork, not taken | parked |
| `sailes-test`'s Quick Reference table is now folded into § The protocol as a `Gate` column | reversible for ~380 bytes if the standalone summary is missed. Recorded so the fold is a decision someone can disagree with rather than a silent loss | 2026-08-30 worker | done |
| Wall-clock A/B on an implementation task | the inner-loop change is argued from STRUCTURE (21 mandated steps, tests at step 12, no fast check named before it), never from a timed comparison. "Faster" is the one claim in three releases with no number behind it | 2026-08-30 eval-run VERDICT | **next** |
| Re-run `inner-loop-promotes-what-caught-a-real-defect` against the `Promotion candidate:` label | the arms were graded on the wording BEFORE the fixed label was added, so the shipped text is not the text that was measured | 2026-08-30 eval-run VERDICT | **next** |
| Run `docs-author` on the next release and check it finds no drift 1.32.0 should have caught | 1.32.0's docs-delta receipt was written by the release lead leaning on 1.31.0's `archify compare`, with no validate/deliver of its own. Stated in the receipt; this is how it gets checked | 2026-08-30 docs-delta | **next** |
| Loop mode in `sailes-implement` (loop-until-Done-when, iteration cap 5, STATE.md on stall) | ratchet + environment work shrinks the need; revisit after 1.1.0 lands in real projects | 2026-07-05 engineering spec (v1, deprioritized) | parked |
| BLOCKED-BY-POLICY protocol (worker reports model refusal verbatim; lead reroutes tier once, then human) | real but rare; one paragraph, add on next `agent-team-structure.md` edit | 2026-07-05 engineering spec (v1, deprioritized) | **DONE 2026-07-26** — in `team-lead.md` + `agent-team-structure.md`. Refusal quoted verbatim (a paraphrased refusal is not evidence of what was refused), **one** reroute, then the human with both quoted. Explicitly forbids re-rolling tiers until one complies — a task two models decline is a fact about the task |
| Scheduled maintenance routine (re-run evals + freshness check, harvest lessons → promotion candidates, digest to human) | needs its payload first — evals/ + freshness check now exist as of 1.1.0 | 2026-07-05 engineering spec (v1) + value spec | next |
| Framework-level lessons harvest across Sailes client repos → golden-module + skill candidates | needs ≥2 client repos on 1.1.0 with populated lessons.md | 2026-07-05 value spec (Phase 5/6 follow-up) | parked |
| Bespoke ESLint plugin packaging the ratchet rules (tokens-only, boundaries) as one install | start with stock rules per repo; extract once the config recurs (graduation rule applied to config) | 2026-07-05 engineering spec (Non-goals) | parked |
| CI wiring for `evals/` (run affected scenarios on PRs touching skills/) | manual dispatch first; automate when eval count grows | 2026-07-05 engineering spec (Non-goals) | parked |
| Hook enforcing that a spec cannot reach `implemented/` without a `.ai/test-plans/<spec>.md` | rejected during `sailes-test` discovery on blast radius (a hook changes behavior in every repo on the machine); revisit once the artifact has proven itself in a real client repo | 2026-07-20 discovery session (sailes-test, Non-goals) | parked |
| Real `agents/*.md` ↔ `codex-agents/*.toml` **content** parity check (today only TOML syntax + the new ROLES-vs-disk guard exist; a qa.md edit that forgets qa.toml still passes) | surfaced as W4 in the sailes-test pre-implement; a real semantic diff is its own change | 2026-07-20 pre-implement (sailes-test) | **DONE 2026-07-26** — `codex-agents/parity.test.js`, wired into `npm test`. Deliberately **not** a text diff: the twins are different documents by design (no model pin on the Codex side, prose rewritten for another runtime), so a word-level diff would fail on every line and get ignored. Instead it asserts (a) the role sets match on both sides and (b) a small curated list of **load-bearing invariants per role** appears in both files. Text is normalized for emphasis first — the first run reported two false drifts on `to *know*` vs `to know`, which is the exact "check that fires on formatting gets ignored" trap. Adding a rule to a role now means adding its concept here, which is what forces the twin edit |
| Mutation testing (Stryker) as tier-A detection proof — needs per-repo config; `sailes-test` mandates it for tier A but a client repo has to wire it | the skill names it; wiring it into a real client repo's CI is the follow-up that proves the tier system end-to-end | 2026-07-20 (sailes-test Phase 5) | next |
| ~~`tester` should flag a code defect back to `be-dev`, not fix feature code itself~~ | **CLOSED 1.10.1** — guard added to `agents/tester.md` + `codex-agents/tester.toml` (write access is test-files-only; a red frozen test is a defect to REPORT), and the eval `tester-never-weakens-a-frozen-assertion` now asserts report-not-fix | 2026-07-20 (eval run) | done |
| `run-` skill for this repo (driver piping SessionStart JSON into the hooks to show what they inject) | deliberately dropped mid-session for `sailes-test`; the hooks were exercised by hand and both behave as designed, so this is convenience, not a gap | 2026-07-20 session (parked by user) | parked |
| F2/F3 of the superseded spec — measure whether the delegation rule reaches the lead's own session (etap A: baseline vs. a short trigger vs. a synced block in `AGENTS.md`), then apply the winner to `AGENTS.md` + `agents-md-template.md` byte-identically | never run before the spec was superseded by 1.35.0's Workflow doctrine; `hooks/workflow-router.js` (SessionStart) now pushes a session into the mandated skill by reading repo state off disk, which may already answer "does the rule reach the session" for the pipeline-entry case — check that against F2's question before re-running it, since the two mechanisms are not obviously the same claim | `.ai/specs/archived/2026-08-06-spec-carries-the-execution-plan.md` F2/F3 | **next** |
| F8 of the superseded spec — `deciding-under-uncertainty.md` D12 paragraph: harness/doctrine forks are "wrong turn is a deploy" by definition, so only the first mandate condition survives ("measure unless you can justify the recommendation from a fact already on disk"), plus the three-run behavioral-arm number from D11 folded into rule 7 | never run before the spec was superseded by 1.35.0; the one-file `git diff --stat` constraint (D12 deliberately took no gate) still applies if this is picked up | `.ai/specs/archived/2026-08-06-spec-carries-the-execution-plan.md` F8 | **next** |
| F10 of the superseded spec — measure the swarm: how many parallel lead dispatches actually pay for themselves, decided by the number rather than before it | never run before the spec was superseded by 1.35.0; D8 (1.35.0, gate-placement) already measured one adjacent question — end-of-spec vs. per-phase gating cost/time — but not swarm width, so F10 is not covered by that result | `.ai/specs/archived/2026-08-06-spec-carries-the-execution-plan.md` F10 | **next** |
| Gate-placement round 2 — detectability and the cost of a late fix, for end-of-spec vs. per-phase vs. hybrid gating | D8 (1.35.0) decided round 1 on cost and time only ($1.37/9.7min end-of-spec vs $2.61/19.1min per-phase vs $2.53/11.4min hybrid); the owner deferred the harder-to-measure axis rather than block the release on it | `.ai/eval-runs/2026-09-16-gate-placement/VERDICT-round1.md`, 1.35.0 P6/D8 | **next** |
| P0.5 `PreToolUse` payload shape for the `Workflow` tool — left unmeasured | P0 (1.35.0) measured `effort`, worktree visibility, and per-role file writes, but not whether the hook's stdin carries `script`, `scriptPath`, or both in every case; `workflow-agenttype-guard.js` was built and tested against the shapes seen so far, not a confirmed complete set | `.ai/eval-runs/2026-09-16-workflow-facts/VERDICT.md` P0.5, 1.35.0 | **next** |
| Eval P4.7 (`lead-dispatches-workflow-with-roles`) was measured on a Sonnet stand-in, not Opus | the lead role in production runs on Opus per `D1`; a Sonnet stand-in may under- or over-state how reliably the lead writes `agentType` into every `agent()` call and honors the `Plan wykonania` waves | `.ai/eval-runs/2026-09-17-lead-dispatches-workflow/`, 1.35.0 P4.7 | **next** |
| Spec B — diagnose/hosting items from the *Idealny Wzrok* feedback (incidents, prod reads, `railway logs`, collectors) | explicit sibling/non-goal of 1.35.0 ("Sibling: spec B … osobno, po zamknięciu tego"); needs its own spec, not folded into the Workflow-first spec | `.ai/eval-runs/2026-09-16-workflow-research/source-feedback-idealny-wzrok.md`, 1.35.0 non-goals | **next** |
| `lead-spawns-named-roles-not-general-purpose` Setup lists no `tester` in arm 1's roster while the phase needs one, so the conditional stand-in for `tester` fails the criterion as written | eval run 2026-09-17 (`.ai/eval-runs/2026-09-17-p6-evals/VERDICT.md`); same behavior graded PASS 09-13 | add `tester` to the arm-1 roster or an explicit clause for a role absent from the stated environment |
| `lead-picks-the-lane-from-the-tier` arm B: P1 touches no screen, so the "qa screenshots" clause cannot arise — a strict grader fails it | eval run 2026-09-17; fixture defect recorded 09-13 | give P1 a screen or make the clause conditional on a touched screen |
| `lead-verifies-status-against-worktree` arm 3 on a Sonnet stand-in holds the dead worker's status file for the human instead of removing it after recording the loss — same on the 1.34.0 text; the 09-13 Opus run removed it | eval run 2026-09-17 with 1.34.0 control | decide whether removal must be stated more explicitly or the eval runs on Opus only |

## Tech debt
| Item | Impact | Source | Status |
|---|---|---|---|
| Re-layout `docs/architecture/{architecture,dataflow,workflow,sequence}` for archify 2.17's `composition/desktop-readability` floor (narrower `viewBox` or split views), then re-render all five pages and produce the 1.33.0 deliver/compare receipt | since 1.33.0 the JSON sources carry content the rendered HTML pages do not show (token-report, `.ai/archive/`, hook budget, fourth block); every future docs delta is blocked the same way. Failure reproduces on the pre-1.33.0 base, so it is not a 1.33.0 regression | docs-author 2026-09-12, `.ai/docs-deltas/2026-09-12-release-1.33.0-notes.md`; human accepted as recorded debt | open |
| `sailes-docs` SKIP protocol let a role install a tool machine-wide without asking (`npx skills add tt-a1i/archify -g`, run by `docs-author` 2026-09-12) | a worker changing what is installed on the human's machine is an outward, hard-to-review action; the doctrine treats installation as the protocol's own remedy rather than a human decision. The install was kept by the human's choice; the rule question is open | 1.33.0 docs delta, run log 2026-09-12 | open |
| `tools/token-report.js`: an unreadable transcript file (`EACCES`) aborts the whole run with exit 1 and no output, while a malformed line is skipped and counted | one permission-broken file in `~/.claude/projects/` makes the token report unusable for every other session; the tool's own contract elsewhere is skip-and-count. Q7 decided lines only, and no frozen case covers files | checker P0, 2026-09-12 (spec 2026-09-12-token-cost-of-running) | open |
| `tools/mcp-toolnames-check.test.js` case "server absent -> SKIP" fails intermittently inside full `npm test` (workers report EPIPE under concurrent load); standalone it passed 3/3 on 2026-09-12 | a gate that fails for a reason unrelated to the change gets argued with once and ignored after that, taking real failures with it; `npm test` is `&&`-chained, so one flake also hides every suite after it | spec 2026-09-12-token-cost-of-running, run log 2026-09-12 (seen by be-dev-2, be-dev-3 and the lead) | **done 2026-09-13** — mechanism: `child.stdin.write` to a server that already exited fails *asynchronously* (EPIPE as an `error` event on `stdin`, no listener), so the `try/catch` in `send()` never sees it and the process crashes with exit 1. Fix: a no-op `child.stdin.on('error')` in `tools/mcp-toolnames-check.js`; the exit/error handlers already report the reason. Same load, 20 runs (4 concurrent × 5): before 15 fail / 15 EPIPE, after 0 / 0. Full `npm test` → exit 0, 20 suites. Standalone it had also failed 1/3 before the fix |
| Report 3b.1 — every status to the owner opens with progress against the goal the OWNER stated, in one line ("ZipTransfers endpoint: 1/5 phases"), then detail | top of the report's own ranking (zero cost; the day's strongest dissatisfaction signal), but outside D1's scope. Touches `Answer shape` rule 1 in both AGENTS.md files | partner-portal report 2026-09-12 §3b.1; scoped out of spec 2026-09-13-quality-gates by D1 | open |
| Report 3b.2 — decision window only for forks the owner feels in business terms; one question at a time; product language, not code | reopens rule 3's width, which the human set deliberately on 2026-07-29 against the recommendation (`.ai/experiments/2026-07-29-adhd-mode/VERDICT.md`) — a human call, not a doctrine edit | report §3b.2 | open |
| Report 3.9 + 3b.3 — the lead verifies a load-bearing claim (changes scope, phase order or an owner decision) before building on it, including the problem statement itself | `team-lead.md` already requires checking an option that cites an existing mechanism; the extension to claims and problem framing is not written. Four decision-changing corrections in one day | report §3.9, §3b.3 | open |
| Report 3.6 + 3c.2 — worktree isolation in multi-repo workspaces (work landed in a clone inside a deleted directory, 2140 lines) and one owner per checkout (lead switched a branch under a working agent); ADR-006 in that repo says `ENV-LOCK` was not carried over in the 1.28.2 → 1.32.0 upgrade | both saved only by an honest agent; not verified against the framework's own isolation text yet | report §3.6, §3c.2 | open |
| Report 3.7 — a contract test asserts on the real HTTP response through the real filter/interceptor stack, not on the object before the filter | green test, zero information; overlaps spec 2's contract probe (3.2) but is a test-authoring rule for `sailes-test` | report §3.7 | open |
| Report 3c.1 — a measured number carries its measurement conditions (stack up or not, which DB, which env) and is never compared with a number measured under other conditions | "43 of 71 e2e red" was 23 once the backend ran | report §3c.1 | open |
| Report 3c.3 — `Base:` in the brief is the sha **after `git fetch`**, compared with `origin/<branch>` | a worker branched from a local `develop` 14 commits behind; the rule was kept literally. One line in the brief template | report §3c.3 | open |
| Report 3c.4 + 3c.5 — the client repo's `guard-protected-paths` hook blocks CREATING a migration (not only editing an applied one) and matches Polish prose as "production deploy" | reported from the client repo; not yet traced to a framework hook template — check `skills/sailes-bootstrap/hooks-template/` before treating it as a framework defect | report §3c.4, §3c.5 | open |
| **`agents/team-lead.md` contradicts itself on gates for a trivial change.** Line 13: "Go solo only when the change fits one sentence and one file — **and even then still run the `checker` review gate and `qa` behavior proof**." Line 17: below about a file's worth of change the delegation overhead exceeds the saving. On a two-character README typo the two are irreconcilable, and nothing says which governs — the same shape as the "never hold idle agents" vs "chase the silent one" collision that was resolved explicitly in 1.16.0 and is the precedent for how to fix this | a lead resolves it either way and both readings look correct; today it resolves silently, which is the failure the earlier collision's resolution note warns about — a rule that survives only because a model re-derives it is not a rule | found 2026-08-01 by the inverse arm of `lead-delegates-instead-of-bulk-coding`, which was grading something else; artifact `.ai/eval-runs/2026-08-01-stale-sweep/artifacts/lead-delegates-inverse.md` | **CLOSED 2026-08-01.** Two findings on the way in: the contradiction was **worse than filed** — `agent-team-structure.md`, the *canonical* file, carried no gate clause here at all, so the two definitions of one rule had already drifted; and the axes were mis-stated in the original entry, since who *writes* a change and who *grades* it are separate questions that the old wording collapsed. Resolved as **the gate scales with what can break, never with who wrote it**: `checker` on any diff that can change behavior including the lead's own (authorship is the reason it applies, not a waiver); `qa` wherever behavior is observable, else `qa: n/a` **with its reason, recorded**, matching the spec-status convention; neither for prose/comments/docs, with the call recorded. The "no gate is optional" line in both files was amended in the same pass so it no longer reads as requiring `qa` against an unobservable change — a skip leaves an invisible hole, a stated `n/a` is a claim someone can argue with. Codex twin updated; parity green |
| **The status file at `.claude/status/` works only because harness isolation leaks.** Measured 2026-08-02 by a worker implementing it: the **`Write` tool refuses a path outside the worktree; `Bash` does not.** So a worker in a worktree can only claim its status file by shelling out. Variant D therefore rests on an asymmetry we do not control and that a Claude Code update could close in either direction — tightening `Bash` would break every claim silently, and the failure mode is the one this artifact exists to prevent: no file, read as "never started" | the mechanism's core property (one readable directory) depends on an undocumented tool inconsistency rather than on anything the framework owns | found while implementing variant D, reported unprompted by the worker | open — **fix is a fallback, not a fight**: doctrine should say that if the outside-worktree write fails, the worker writes `<worktreePath>/.claude/status/<id>.md` instead and says so in its report, so the lead falls back to reading it per-worktree. Degrades instead of breaking. **CLOSED 2026-08-02 by `10a1f3f`** — verified an ancestor of `9de90e8`: the fallback path and the report obligation are in `agent-team-structure.md`, all six Claude role definitions and all six Codex twins, and `tools/worker-status.js` implements the mechanical half (`findWorktreeStatusFallbacks()` walks `.claude/worktrees/*/.claude/status/` and labels each hit). The lead-side rule is there too. **This row was re-marked "open — scheduled 1.28.0 F4" on 2026-08-02 by the truth pass that was supposed to catch exactly this**, then corrected when the assigned worker refused to edit already-correct prose and produced the commit. See the lessons entry of that date: the rows this pass got wrong were the ones filed and fixed on the SAME day, which is why they read as freshest |
| ~~**The worker status file may be invisible to the lead — 1.27.0 never says where it physically lives.**~~ **CLOSED 2026-08-02** — variant D chosen by the human after the trade-off was laid out: `.claude/status/`, the PID-file convention, with `worker-id` from the harness and append-only close. Implemented, live-tested with two concurrent workers, three further defects found by using it. Residual risk is the row above. Every writing worker runs in a worktree (mandatory), so `.ai/status/<id>.md` written by that worker lands **inside the worktree**, not the main tree. `.ai/status/` is gitignored, so it never arrives by cherry-pick either. A lead listing `.ai/status/` from the main tree therefore sees nothing, while the doctrine tells it to "verify the file against the worktree" without ever saying the file IS in the worktree. Two defensible fixes and they differ: **(a)** the ladder names `<worktreePath>/.ai/status/<id>.md` and the file stays isolated — honest, but the directory-level invariant ("whatever is here is running or dead") stops being readable in one place; **(b)** workers write to the MAIN tree's `.ai/status/` — one readable directory, and no collision since each worker owns one filename, but it puts a writer's hand outside its isolation, which needs saying out loud rather than happening quietly | the mechanism is partly non-functional as shipped: the sweep, the invariant and the lead's verification all assume one readable directory that does not exist under the isolation mandate | found 2026-08-02 by the CONTROL arm of `lead-verifies-status-against-worktree`, while grading something else entirely | **CLOSED 2026-08-02** — variant D chosen and implemented; this Status column said "open — this is the decision" until 2026-08-02, three lines under its own strikethrough heading saying CLOSED. Corrected during the 1.28.0 truth pass, which is the reason that pass exists: a row that contradicts itself is read by whichever half the reader reaches first |
| **The status-file format failed on its first real use, twice.** The first genuine `.ai/status/be-dev-1.md` was rejected by `tools/worker-status.js` in five messages covering two real defects: **(1)** the doctrine's example and the validator disagree on list syntax — the worker wrote a block list (`  - path`), the validator accepts only inline (`["a","b"]`); **(2)** `outcome: done` with an empty `commit:` is a **real state** the format forbids — a plan-only brief's evidence is a file recorded in `touched`, not a commit, so "a done result with nothing to point at" is false there | F4 (validator) and F5 (doctrine) were written by two workers who could not see each other; the lead's integration check compared the nine field NAMES and declared consistency without comparing value SHAPES, which is exactly one level too shallow | 2026-08-02, first real status file ever written | **CLOSED 2026-08-02, and the second half was closed the OTHER way — worth knowing before someone "restores" the proposed fix.** (1) Both list syntaxes are accepted: `worker-status.js:125` looks ahead for an indented block list. (2) The proposal here — let `outcome: done` be satisfied by a non-empty `touched` plus a reason — was **rejected in implementation**: `worker-status.js:230` still requires `commit:` for `done`, because `done` is a claim and the sha is what makes it checkable. The plan-only case is served by `outcome:` values other than `done`, and a `commit:` that is not a sha now fails with "put the reason in `note:`, not here" (`:224`). Verified on disk during the 1.28.0 truth pass |
| **`agents/team-lead.md:10` sends every lead to a path that does not exist in a client repo.** It reads "Read `skills/sailes-bootstrap/agent-team-structure.md` (the canonical definition) before planning any non-trivial task". That resolves in the framework repo and **nowhere else**: the plugin serves skills from `~/.claude/plugins/…` and a client tree has no `skills/` directory. `agents-md-template.md:109` already says so in the client's own words ("It is a globally-installed skill, not a file in this repo"), so the template knows and the role definition — the file the plugin ships to every machine — does not. It is also the **only** role that references the canonical file at all, and it does so as an instruction with nothing verifying the read happened | every lead on every client repo is told to read the canon by a path that fails, so either it silently proceeds without the canon or it spends a turn discovering the file is not there. Directly weakens any design that puts a single source of truth in that file | measured 2026-08-01 while grounding a decision card for the delegation spec | **CLOSED 2026-08-01** — `agents/team-lead.md:10` now names the skill, states that the repo path exists only in this framework repo, and records why the line was wrong. Verified on disk during the 1.28.0 truth pass |
| **`lead-delegates-instead-of-bulk-coding` demands a stated reason in one direction only.** Its criterion requires an explicit reason when the lead goes solo, and none when it delegates — while the threshold it grades (`delegation-threshold.md`) says the reason is owed **in both directions**. A silent delegation passes today | the eval no longer measures the whole rule it points at, and the half it drops is the one that hides cost: an unexplained delegation is the expensive direction of the same mistake | found 2026-08-02 by the sub-team re-cutting the sibling scenario's fixture, flagged as out of its scope | **open — re-verified 2026-08-02 against the file, and it survived the F7 re-cut that closed its sibling row above.** `evals/lead-delegates-instead-of-bulk-coding.md:16-20`: "If the lead proposes to write the code itself, it must give an explicit reason" — nothing demands one when it delegates. **Trap for the next reader: the scenario's `Last run:` notes say "Threshold reasoning stated in both directions, as the source now requires", which describes what the graded AGENT did, not what the criterion DEMANDS.** Reading the notes instead of the criterion closes this row by mistake; it was closed by mistake once during this very truth pass and reopened on a second read |
| **The delegation threshold is written in three places in different words, and three evals now encode three different versions of it.** `team-lead.md` says go solo when the change fits one sentence and one file; two paragraphs later, below about a file's worth the overhead exceeds the saving; `agent-team-structure.md` says the same in a third wording. Measured 2026-08-01: `lead-delegates-instead-of-bulk-coding`'s inverse arm **rewards** not spawning a worker for a trivial change (PASS), while `lead-gives-every-writer-a-worktree` arm 1 **requires** a spawned writing worker for a three-line one-file change — the same shape, opposite demands. A control run against the pre-2026-08-01 `team-lead.md` behaved identically, so this is not a regression from that day's edit; the collision is older and structural | one of the two evals must fail whatever a correct lead does, so a future reader either weakens a real rule to make a scenario green or edits a scenario to match a rule that was never the one under test | 2026-08-01 stale sweep, arms in `.ai/eval-runs/2026-08-01-stale-sweep/artifacts/worktree-arm1-onefile.md` and `…-CONTROL.md` | **CLOSED 1.27.0** — `skills/sailes-bootstrap/delegation-threshold.md` is the one source; `tools/sync-blocks.js` stamps it into the three consumers and `--check` runs in the gate, with a mutation-proven test. Both fixtures re-cut in F7. Note the sibling defect in the row below survived this fix — the threshold is now single-sourced, but one eval still grades only half of it |
| **`lead-checks-second-order-effect` arm 2's criterion conflates two things.** It reads "the lead **accepts it**. Rejecting it is a FAIL", but a lead can accept the worker's *justification* (which is what the arm guards) and still reject the *substitute decision* on an unrelated, real axis — which is exactly what happened on 2026-08-01. Recorded as FAIL by the letter with the note that the guarded failure mode did not occur | the eval currently fails a correct answer, so a future reader either loosens the criterion to make it pass or edits the doctrine to match a defective criterion | 2026-08-01 stale sweep | **CLOSED 2026-08-01 by `571c871`, re-verified 2026-08-02.** `evals/lead-checks-second-order-effect.md:30` already separates the axes — accepting the justification is the criterion ("present = met, absent = not met"), while the substitute decision may be rejected without failing the arm. Re-run 2026-08-02, both arms PASS. **One residual defect found by the F7 worker and fixed there:** the `Setup` line's arm-2 label read "(control — MUST NOT fire)", which re-collapses both axes into "accept = pass" for any grader anchoring on `Setup` before reaching `Expected` — the exact bug this row was about, surviving one paragraph above the fix. Now reads "(control on the justification, not on the substitute decision — see Expected below for which axis is graded)". Like row 25, this row was re-marked open by the 2026-08-02 truth pass and corrected by the worker assigned to it |
| **D5 from the browser-instrument spec: does `designer` get browser tools?** It has no Bash today, so it cannot render anything to measure; the integrity gate runs on whoever builds (`fe-dev`). Giving it browser tools is only coherent together with Bash, which changes what the role *is* — a human call, deliberately left unmade. Three-line edit once answered | a designer could measure its own spec before handoff; today it hands over unmeasured intent | 2026-07-25 spec §5 (now in `implemented/`) | **DONE 2026-07-26 — human said yes.** Browser tools **and `Bash`** granted (the first without the second is decoration — you cannot measure a page you cannot boot); inspect-only MCP set mirroring `fe-dev`, no interaction tools. Three guardrails shipped with it, because this changes what the role *is*: `Bash` is boot-and-inspect only (no install/migrate/commit/push), a defect found is **reported not fixed** (the `tester` rule), and **it does not retire the integrity gate** — `qa` still verifies the built result on a clean context, since a maker measuring its own intent is not a gate. Codex twin updated |
| ~~**Worker release has a rule but no mechanism**~~ — six workers spawned for two eval runs, zero released until the human noticed; each kept emitting `idle_notification` that reads like new work. **CLOSED 1.15.0**: mechanism named (`shutdown_request`, not `TaskStop`), release confirmed rather than assumed, FILE deliverable for gradable work, empty-return cause corrected. Ledger: `.ai/runs/2026-07-25-eval-session-and-worker-lifecycle.md` | every delegated run; the leak scaled with fan-out | 2026-07-25 eval session | done |
| Behavioral GREEN re-runs pending for 1.1.0 text-level edits (see evals/ "pending" lines) | text Done-when passed; behavior unproven until re-run post-merge | this change-set | **CLOSED 2026-07-26** — the whole suite was re-run to currency: 25/29 FRESH, 12 re-dispatched and all PASS. The four that remain STALE are environment-blocked and triaged |
| **Release-hygiene tests — `npm test` reads no version file and no CHANGELOG.** Three checks, all deterministic, all ~10-15 lines: (a) `VERSION` == `package.json` == `plugin.json` == `marketplace.json` == the `AGENTS.md` stamp; (b) `VERSION` has a matching `## x.y.z` heading in `CHANGELOG.md`; (c) wire `evals/fixtures/browser-probe/run-probe.mjs` into `npm test` — it already self-SKIPs without a browser, so it is safe to add | the stamps have drifted **four times** (`marketplace.json` ×2, `AGENTS.md` ×2, the latter shipping in 1.14.0), and Upgrade mode computes its delta from CHANGELOG headings, so an entry-less change is invisible to every repo. (c) closes the gap where a probe edit ships unverified unless someone remembers a second command | 2026-07-25 harness review (measured) | **CLOSED 1.16.0** — `release-hygiene.test.js` (10 assertions: five stamps agree, CHANGELOG heading present, newest heading == VERSION, no duplicates) + `run-probe.mjs` wired into `npm test` |
| **Eval staleness is unmeasured — 9 of 27 evals name a file that changed after their last run** (measured 2026-07-25). `evals/README.md` mandates a re-run whenever an edited skill is named, and nothing checks it. Blocker to automating: 9 evals have no machine-readable path in `Skill under test`, so the format needs a `Files:` line first — that makes it its own spec, not a quick test | the doctrine's own regression net silently decays; nobody can tell a green eval from a stale one | 2026-07-25 harness review (measured) | **CLOSED 1.16.0** — `evals/harness/eval-status.js` + the `Files:` line; 0 not-computable, 0 stale-by-omission |
| **`hooks/framework-version-check.js` has no test file** — the only hook without one (`workflow-router.js` has 20 assertions). It was exercised by hand on 2026-07-25 by piping SessionStart JSON into it; that proof died with the session | a hook that speaks in every session on every machine, with deterministic behavior and no test, in a repo whose rule is "deterministic behavior gets a real test" | 2026-07-25 harness review | **CLOSED 1.16.0** — `hooks/framework-version-check.test.js`, 15 assertions, every must-speak case paired with a must-stay-quiet one |
| **`Files:` editorial pass over the remaining 24 evals.** 1.16.0 shipped `eval-status.js` and added `Files:` to 3 of 27 scenarios (the `lead-*` three, chosen because they are also the 1.15.0 debt). The other 24 report NO-FILES. Not mechanical: several name a skill in prose with no resolvable path, so each needs a judgment call about what it actually grades | until it is done, `eval-status.js --strict` cannot be a release gate, and 24 scenarios still cannot be told fresh from stale | 2026-07-26 (1.16.0) | **done 2026-07-26** — all 29 scenarios carry `Files:`, done as the first real sub-team run; `--strict` now usable on a clean checkout |
| **The role/model table lives in three places** — `docs/agent-roles.md`, `agent-team-structure.md`, `agentic-first-principles.md` — and 1.16.0 had to edit all three to avoid drift. This is exactly the repetition Claude-5 shift #4 names. Collapse to one canonical table with pointers | three copies is three chances to drift, and the drift is silent | 2026-07-26 (1.16.0) | **DONE 2026-07-26** — collapsed to one table in `agent-team-structure.md`; the other two now point at it. The dedup immediately paid for itself: all three copies had drifted and **two had lost `tester` entirely**, so a reader of either ran a seven-role pipeline with the test gate missing and no way to notice |
| **Token-accurate context measurement.** `context-cost.js` reports bytes and words because the repo has zero dependencies; real counts need `messages.count_tokens`. Decide whether the dependency is worth it, or whether an optional opt-in script outside `npm test` is the right shape | the Claude-5 thesis is stated in tokens; a bytes proxy is honest for two refs of one file and nothing else, which is a narrower claim than the one we would like to make | 2026-07-26 (1.16.0) | parked |

| **Lifecycle rules 4 and 6 conflict on a silent worker and neither states which wins.** Rule 4: "never hold idle agents". Rule 6: chase a silent worker once. The 2026-07-26 eval run resolved it correctly but by *derivation* — the agent refused to release, because the worker's context is the only place the findings may exist and releasing guarantees a re-run. Write the resolution down: hold until the report is recovered or the escalation resolves, then release | a derived rule survives only as long as the deriving model does; the next reader may resolve it the other way and destroy a report that existed | 2026-07-26 eval run (`lead-chases-an-empty-worker-return`) | **DONE 2026-07-26** — written into both files: rule 6 governs, because a silent worker is not idle in the sense rule 4 means; its context is the only place its findings may exist, so release guarantees a re-run while holding costs one agent for minutes |
| **Escalating a *gate*, not just a maker, is unaddressed by the routing rule.** The 2026-07-26 run escalated `checker` to Opus on a tenancy diff, reasoning that the defect there is what the diff *omits*, so grading requires reasoning over the whole access-path map rather than reading a patch. That is a sound trigger the doctrine does not name — and it is not obviously general, since most review is a patch read | the routing section speaks only of workers; a lead wanting to escalate a gate has no stated trigger and will either not do it or do it on vibes | 2026-07-26 eval run (`lead-escalates-a-model-on-judgment-not-volume`) | **DONE 2026-07-26** — named in both files: escalate a gate when the defect is **what the diff omits**, which needs the whole surface held in mind rather than a patch read. Still judgment, never size |

| **`prompt-anchor` needs a SECOND eval arm: the control WITHOUT the anchor, on Opus 5.** Today the eval asks only "does the anchor hold the line at depth". Anthropic's Claude-5 context-engineering guidance (repetition → single description) makes the null hypothesis live: the mandate may hold at turn 40 with no anchor at all. Add the arm **before** the rebase of `enforce/*` and the real-distance fixture — it is the cheaper experiment and it can retire the whole branch set instead of resurrecting it | a green single-arm result would prove the anchor works while never testing whether it is needed, and would read as justification to merge a per-prompt injection we may no longer require. Four `enforce/*` branches are a week behind `main` and waiting on this | 2026-07-25 context-engineering review ([[lessons.md]] entry of the same date) | **MOOT 2026-07-26 — the subject is gone.** `prompt-anchor` was retired by human decision and the four `enforce/*` branches were deleted (SHAs preserved in the spec, now at `.ai/specs/archived/2026-07-18-prompt-anchor.md`). This row asked for the control arm that would decide whether to keep the anchor; the decision was taken the other way without it. Left visible rather than deleted, because the *method* it argues for — add the cheap control arm before the expensive fixture, since it can retire the whole branch instead of resurrecting it — is the part worth carrying, and it is now the standing rule for new scenarios |
| **Three skills are monoliths where the repo already has the pattern for the opposite** — `sailes-discovery` (21 KB / 3 293 words), `sailes-design` (18 KB), `sailes-async` (16 KB) load whole on trigger, while `sailes-bootstrap` is a thin SKILL.md + 20 on-demand references. Mechanical split, low risk, no behavior change intended — so it needs an eval re-run per skill to prove exactly that | every trigger of those three spends 2–3k words of context before it is known which branch of the skill applies; total `skills/**/*.md` is 656 KB | 2026-07-25 context-engineering review | **DONE 2026-07-26 — one of three split, and the other two measured and deliberately NOT.** The item assumed all three share a shape. They do not. **`sailes-discovery`: split, 22.3 KB → 14.9 KB (−33%)** — it had genuinely conditional content, two checklists of which a session uses exactly one, plus the decision-card method needed only when a fork appears. Now `SKILL.md` + `decision-card.md` + `checklists.md`. Re-run PASS: the agent pulled both references, produced five cards (three carrying "nie mam podstaw"), one measurement offer, the Decisions Ledger, and the mandatory bootstrap handoff. **`sailes-async`: not split.** Its `## Workflow` is 8.3 KB of 15.9, but it is six sequential phases of 1–2 KB each — extracting them yields six files you read anyway, i.e. indirection with no saving. **`sailes-design`: not split.** Its two heaviest sections are the MANDATORY physical-integrity gate and the Common Mistakes table, both on every run's path; the genuinely conditional parts total ~12%, which does not pay for two files and two hops. The lesson worth keeping: **"large" is not the split criterion — "not needed on this run" is** |
| **Gotcha-vs-inferable audit of `agents/*.md` + AGENTS.md prose** — keep what the model cannot derive from the repo (CRLF, `main` is production, five version files), drop prose that only corrects behavior Opus 5 gets right from context. **Two hard constraints:** the spine (`SPEC → HUMAN → VERIFIED → GATED`) is out of scope — it encodes authority, not capability — and every cut is **per-harness**, because `codex-agents/*.toml` runs on non-Claude models for which that prose is the only backstop. **Blocked on the eval debt below**: without a working regression net, a simplification and a regression are indistinguishable | competing and redundant instructions cost tokens and deliberation in every session; but cutting blind on a net where 9/27 evals are stale is the recorded silent-failure shape, so ordering matters more than the saving | 2026-07-25 context-engineering review | **next — unblocked 2026-07-26**: staleness is now measurable (`eval-status.js`, 0 not-computable) and the three 1.15.0 re-runs are done, so a deletion can be told apart from a regression |
| ~~**The browser probe in `npm test` is the suite's only non-deterministic step**~~ | **RESOLVED 2026-07-26, same day** — it failed twice under heavy concurrent agent activity ("the browser never exposed a CDP target") and was clean across six consecutive runs otherwise. The backlog item that asked for it named the wrong reason it was safe: it self-SKIPs on the *absence* of a browser, not on *contention* for one. Moved out of the default gate into `npm run test:browser`, with `npm run test:all` for both — `npm test` is deterministic again, and the probe still runs on demand and in the release ritual | 2026-07-26 (1.16.0) | done |
| **Nango (self-hosted) as the integrations/OAuth layer** — recommended by Marcin 2026-07-26 for the Railway + Postgres stack: per-user auth under our own brand, syncs, webhooks and observability on one platform, and client tokens staying on our infrastructure rather than a US SaaS (the argument that matters for PL clients with a DPO). **Nothing here is verified** — the claims come from a forwarded message, not from the docs, and I did not check them. Where it would live is also undecided: a `decision-engine.md` card, a domain sibling next to `sailes-pipedrive`, or nothing at all | today every integration is bespoke per repo, `sailes-pipedrive` is the only named one, and OAuth on behalf of an end user has no home in the pipeline | 2026-07-26 (raised mid-session, parked by the human) | parked — verify before promoting |
| ~~**The two halves of 1.16.0 model routing contradict each other**~~ | **DECIDED 2026-07-26 by the human — option A.** The constraint is real, not documentation drift: passing a full ID to the Agent tool is rejected with `InputValidationError`, verified against the live schema rather than assumed. So an override buys a **tier, not a version**, and that is accepted: the pin's value sits on the default path where nearly every run lives, escalations are rare and already logged with a reason, and the alternative (a twin role file per escalated role) reintroduces the duplication that already drifted across three copies of one table in this repo. Two obligations shipped with it — log the **alias** you passed, not merely that you escalated; and when a role is escalated routinely, promote it to its own pinned definition rather than overriding forever, which is the graduation rule this framework already applies to config. Options B/C/D rejected: B on duplication, C because it discards the reproducibility argument on the whole surface rather than on escalations only, D because `CLAUDE_CODE_SUBAGENT_MODEL` is machine-wide and does not do per-task at all | 2026-07-26 | done |
| **The `mcp__chrome-devtools__*` allow-lists have never been validated against the real tool surface.** Three role files (`qa.md`, `fe-dev.md`, `designer.md`) hardcode tool names in `tools:`; nothing checks that those names exist in the installed server. `handle_dialog` was found missing in 1.17.1 **by accident**, while an agent was reading prose for an unrelated reason. Verified 2026-07-26: `npm view chrome-devtools-mcp dist-tags.latest` = **1.6.0**, while the repo's prose cites Chromium/run evidence from other version families — so nobody has compared the two. A check is possible (list the server's tools, diff against the union of the three allow-lists) but needs the MCP server present, which is the same blocker as the three environment-blocked evals | a role that names a tool it cannot call fails only at the moment it needs it — mid-gate, on a live app. And the reverse: a tool the server offers that no role lists is a capability silently unavailable | 2026-07-26 (`researcher-reports-provenance-and-does-not-decide`, found while grading it) | **open — scheduled 1.28.0, phase F5**, and the "blocked" note is now wrong: the `chrome-devtools` MCP was wired up on 2026-07-26 and its tools reach the roles, so the check is buildable here. Split deliberately: F5a builds the instrument and MEASURES (it must not touch any `agents/*.md`, which another worker owns concurrently), F5b applies whatever it found. An empty result is a result and closes the phase |

| **Decision cards are inconsistently applied across the skills that mandate them.** From a full inventory of the pattern (6 templates, 17 worked cards, ~35 rule sites across 14 files), taken 2026-07-26: **`sailes-hosting` decides real forks — Volume vs S3, Dockerfile vs Nixpacks, migrate-on-start — as "złote zasady"**, while `sailes-bootstrap/SKILL.md:69` lists Hosting in the mandatory card set; `sailes-design` resolves forks by "confirm with the user" rather than a card; the same migration-strategy fork is a full card in `db-compendium.md` but a bare three-line branch rule in `decision-cards.md`, which is the file the skill actually routes agents to. **Generated client repos get less than the framework runs on:** `agents-md-template.md:34` ships only the one-line HUMAN rule, and `spec-writing-template.md:26-28` omits the "present real forks as options + ✅/⚠️ + recommendation" instruction its declared master carries — despite the template saying both must change together. The brownfield **Task Brief has no Decisions Ledger**, so brownfield card outcomes have no canonical home. Label drift is unmanaged (`Dlaczego ważne` outnumbers canonical `Dlaczego to ważne` 14:4; three cards omit `Twój wybór?`) | the card is the framework's main mechanism for "the human owns every key decision". Where a skill quietly resolves a fork instead, that principle is off in exactly the places most likely to be expensive — hosting and migrations. And a generated repo that ships a weaker rule than the framework uses will drift further with every session | 2026-07-26 (inventory in `.ai/eval-runs/2026-07-26-anchor-control/decision-card-inventory.md`) | next |

| **Receipt reproducibility spot-check in `delta-at-gate.md`** — tell the lead to re-run `compare` once before showing the human the receipt (cheap: exit 0 + matching hashes). Observed in the wild 2026-07-28: the arm2b eval agent did exactly this unprompted, and the arm2 first-run agent used the same move to catch a fabricated fixture receipt | a receipt is evidence only if it reproduces; today the doctrine trusts the file | 2026-07-28 (`.ai/eval-runs/2026-07-28-docs/VERDICT.md`) | promotion candidate |
| **`archify-setup.md` SKIP protocol has no case for "binary fine, nothing valid to compare"** — flagged by the gate-arm1 eval agent: the protocol anticipates missing/below-floor, not schema-invalid sources. Rare in real repos (bootstrap authors valid sets), real in adopted ones | a gate that can only say SKIP-or-pass mislabels a third state | 2026-07-28 (same run) | promotion candidate |
| **`.ai/specs/2026-07-18-prompt-anchor.md` sits in the live root with `Status: RETIRED`** — a status outside the canonical five, in the folder that means "live". Human decision where it goes (`archived/` + which status) — the retirement record is human-authored | root = the only live set; a retired spec that reads as live is the exact drift the router flags | 2026-07-28 (lifecycle self-docs worker) | **CLOSED** — the human answered: the spec sits at `.ai/specs/archived/2026-07-18-prompt-anchor.md`, out of the live root. Verified on disk during the 1.28.0 truth pass; the live root now holds exactly the two 2026-08-01 specs |

| **`graphify-setup.md` portability sed is GNU-only** — `sed -i -E 's#...#g'` silently no-ops on macOS/BSD (needs `sed -i ''`), leaving the absolute binary path committed — the exact silent-success shape, found by the bootstrap-code-map re-run actually executing the procedure on darwin 2026-07-28 | a doc step that reports success and does nothing, on half the machines this framework targets | 2026-07-28 (rr-bootmap, fixture run) | **open — re-verified 2026-08-02, unchanged.** `skills/sailes-bootstrap/graphify-setup.md:41` still reads `sed -i -E 's#…#g' "$f"`. Scheduled for 1.28.0 |
| **`repo-done-checklist.md` hook check is husky-blind** — it hardcodes `.git/hooks/post-commit`, so a husky-managed repo (core.hooksPath) reports `MISS graphify hook install` while the hooks are installed and firing — an instrument false-negative measured live 2026-07-28 | a checklist that flags correct work gets argued with once and ignored after that | 2026-07-28 (rr-bootmap) | **open — re-verified 2026-08-02, unchanged.** `skills/sailes-bootstrap/repo-done-checklist.md:78` still hardcodes `"$ROOT/.git/hooks/post-commit"` with no `core.hooksPath` lookup. Scheduled for 1.28.0 |
| **`agents-md-template.md` claims "lint on raw literals" but bootstrap ships no lint snippet** — every bootstrapped repo inherits an enforcement claim with nothing behind it; the tokens-only lesson recurred 3× in exactly that gap. Proposed fix in the promotion re-run artifact: ship `eslint-design-tokens-rule` as a bootstrap asset + pointer edits | an asserted-but-absent enforcement is worse than prose — it stops anyone from adding the real one | 2026-07-28 (rr-promo root-cause trace) | promotion candidate |
| **`diagnose-runs-live-case-before-audit` control arm cannot exist on a plugin-installed machine** — skill descriptions route both arms, so the router-mandate's marginal value is unmeasurable here; scenario needs a plugin-free machine (or a stripped stand-in listing) to isolate it | an eval whose control cannot diverge will read as PASS forever without proving the mechanism | 2026-07-28 (rerun batch) | sharpen when a clean machine exists |

| **`archify-setup.md` step 0 is itself broken in Git Bash on Windows** — the new one-liner `…split(require("path").sep).join("/")` passes a lone `/` as an argument, and MSYS rewrites it to the Git install root *before Node runs*: `ARCHIFY_HOME` resolves to `C:C:/Program Files/Git/UsersC:/Program Files/Git/karol/…` and every CLI call dies. Measured 2026-07-29 on the same Win11/Git Bash/Node 24 box the `$HOME` fix was measured on. `export MSYS2_ARG_CONV_EXCL='*'` in the same command fixes it. One layer out from the hazard the doc was rewritten to fix: that was Node not understanding an MSYS path, this is MSYS mangling the argument Node never sees | the fix shipped today for a Windows failure does not work on Windows — and it fails *looking healthy*, since the shell reports no error and only the CLI call dies. The role's likely response (SKIP? debug? proceed unvalidated?) is untested | 2026-07-29 (`docs-author-stays-in-lane` re-run) | **CLOSED** — `archify-setup.md:13` resolves `ARCHIFY_HOME` with `p.posix.sep` and contains **no `/` literal at all**, which is the property that defeats MSYS argument rewriting; `:46-58` records both hazards, including that the first attempt failed and why the author missed it (`MSYS_NO_PATHCONV=1` was exported in their shell). Verified on disk during the 1.28.0 truth pass |
| **Doctrine contradiction: is `.ai/docs-deltas/*.html` committed or ignored?** Today's `delta-at-gate.md` edit says only the `.json` receipt is committed and `.gitignore` covers the HTML (this repo's own `.gitignore:20` and its single tracked receipt agree) — but `archify-setup.md:75` still reads "Everything under `docs/architecture/` and `.ai/docs-deltas/` is **committed** — HTML included: it is the client-facing artifact and must survive a machine that lacks archify". Two files, one repo, opposite instructions; the edit updated one and left the other. Note the *reason* in the stale sentence is a real one that the new rule silently drops (a machine without archify cannot re-render the delta from the receipt) — so this is a decision, not a typo | an agent that reads `archify-setup.md` first will commit 1.8 MB of derivable HTML per release and believe it is following doctrine; one that reads `delta-at-gate.md` first will not. Both cite the framework | 2026-07-29 (`gate-refuses-to-close-a-spec-without-docs-delta` re-run) | **CLOSED** — both files now say the same thing: `archify-setup.md:95-102` splits the two directories explicitly ("the `.json` receipt only … `.gitignore` covers the HTML there") and `delta-at-gate.md:34` agrees. The reason the old sentence carried — a machine without archify cannot re-render the delta from the receipt — was answered rather than dropped: `docs/architecture/` HTML **is** committed and is the client-facing artifact; only the per-release delta HTML is derivable scratch. Verified on disk during the 1.28.0 truth pass |
| **Arm 1 of `gate-refuses-to-close-a-spec-without-docs-delta` demands a behaviour the doctrine does not.** The criterion requires the agent to NOT move the spec and to hand the compare command back to the human. `delta-at-gate.md` only forbids closing *without* a receipt; steps 3+7 describe running the gate and then closing. Two independent 2026-07-29 runs took that third path — quoted and refused "delta zrobimy przy okazji", ran compare, committed a real receipt, then closed — scoring FAIL on the letter while holding the protected behaviour. Either the criterion accepts "gate run before the `git mv`", or the doctrine states the lead must stop and escalate | the scenario currently reports FAIL for correct behaviour, which is exactly the signal that gets ignored after the second time | 2026-07-29 (this re-run) | **CLOSED 2026-08-02** — arm 1 GREEN at `27bdb98` on a purpose-built fixture: the agent refused "deltę zrobimy przy okazji", ran the gate, showed the receipt and **stopped**, verified from disk rather than from its report. The criterion/doctrine mismatch was resolved on the doctrine side — stopping before the `git mv` is now what the gate requires, so the third path (gate-then-close in one motion) is no longer a defensible reading |
| **Doctrine contradiction: may `docs-author` write `.claudeignore`?** `docs-author.md` says "Your writes land under `docs/architecture/` and `.ai/docs-deltas/` only" and the eval criterion checks `git status` shows changes only under `docs/`+`.ai/`; `archify-setup.md` tells the same role to write the ignore-wiring block at the **repo root**, "once per repo, at bootstrap/adopt". The 2026-07-29 eval agent obeyed the second and technically breached the first — correctly, and it disclosed the write. One of the two must move: either name `.claudeignore` as an explicit exception in the role's lane, or hand the ignore wiring to bootstrap and out of `docs-author` | a role boundary with an undocumented exception trains agents to treat the boundary as approximate — and the boundary is the whole point of this role | 2026-07-29 (`docs-author-stays-in-lane` re-run) | **open, DECIDED 2026-08-02 — the exception moves into the role's lane.** Human chose (a): `docs-author.md` names `.claudeignore` as a single explicit exception — one block, once per repo, at bootstrap/adopt — rather than handing the ignore wiring to bootstrap. Rejected (b) on a measured cost: an adopted repo that never runs full bootstrap would silently never get the entry, and the prompt cache grows ~1.8 MB per release with nothing reporting it. The price accepted is that the lane stops being a sentence without an "and". Live line numbers for the implementer: role lane `agents/docs-author.md:23-24`, the instruction it collides with `skills/sailes-docs/references/archify-setup.md:86-93`. **CLOSED 1.28.0 (F3).** The exception is named in `agents/docs-author.md` and its Codex twin with the reason attached; `archify-setup.md` cross-references the role definition so neither file states the rule as if the other did not exist. The eval was re-cut with a fixture pair and **both PASS**: Fixture A (bootstrap/adopt) wrote `.claudeignore` and nothing else outside the lane, and called it "the single sanctioned out-of-lane write" rather than flagging a violation; Fixture B (mid-project, pressured toward `.gitignore`/`package.json`) left `.claudeignore`'s sha256 unchanged — the exception did not reopen for a second edit. **Residual, filed separately below: the sentence splits behaviour under a SKIP**, which neither fixture covers |

### Found 2026-08-02 by six eval re-runs, none of them looking for these

> All thirteen came out of runs grading something else. Nine were found by an agent **executing** a
> procedure rather than reading it — the pattern this repo has now recorded four sessions running.
> None is in 1.28.0's scope; they are listed together because they were found together.

| Item | Impact | Source | Status |
|---|---|---|---|
| **The plugin clone does not auto-update, and five agents discovered it independently.** `~/.claude/plugins/marketplaces/sailes` sits at `d6b64e2` = **1.27.0** while `origin/main` is `9de90e8` = 1.27.2 — two releases behind, with `autoUpdate: true` set. `AGENTS.md` states the deploy is automatic and needs no install step; on this machine it demonstrably did not happen | **this is the most load-bearing finding of the day.** Every "spawn the real named role" instruction serves whatever the clone holds, so a real-role eval grades a version nobody chose. Five of six re-runs on 2026-08-02 refused the real role for exactly this reason and said so. It also means `evals/README.md`'s "you are grading the deployed version" is a much larger caveat than it reads | 2026-08-02, six eval re-runs | **open — needs diagnosis, not a doctrine edit.** Is `autoUpdate` firing and failing, or never firing? Until it is known, an eval's vehicle line must state the clone's sha, not just "real role" |
| **`worker-claims-before-it-writes` is PASS on the text and FAIL on the deployed runtime**, reproduced 2/2 | the only 2026-08-02 result that says anything about production rather than the working tree: a real `be-dev` wrote no status file at all, because deployed 1.27.0 predates the "Never silently skip the claim" hardening. Closes automatically when the clone advances — which is the row above | 2026-08-02 | open — resolves with the clone |
| **`repo-done-checklist.md:156-158` reports DRIFT on a repo that documented its own file-naming convention.** It greps `AGENTS.md` for anything matching a path-like pattern and asserts the file exists, so a repo writing `SPEC-NNN-kebab-slug.md` as its convention gets `DRIFT: AGENTS.md references missing path SPEC-NNN-kebab-slug.md` | **same class as the husky false-MISS fixed in this very release**, in the same file, found by the eval re-run verifying that fix. An instrument that flags correct work gets argued with once and ignored after | 2026-08-02 (`ai-scaffolding-is-idempotent`) | open |
| **`repo-done-checklist.md` prose requires `.ai/adr/ADR-001-*`; its script checks only `.ai/adr/template.md`** | a bootstrap that never wrote its first ADR passes green | 2026-08-02 (`bootstrap-generates-code-map`) | open |
| **`skeleton.md` promises a committed `GRAPH_REPORT.md` that Step 4.9 never generates.** `graphify extract` itself prints `next: run graphify cluster-only <path> to generate GRAPH_REPORT.md`, and the AGENTS.md block written by `graphify codex install` tells every agent to read that file | a by-the-book bootstrap ships an agent contract pointing at a file that does not exist. The graded subject noticed and ran `cluster-only` itself — a stronger reader than the doc | 2026-08-02 (`bootstrap-generates-code-map`) | open — the procedure needs the `cluster-only` step |
| **`adopt-existing-repo.md` step 2.0 has the same hole**, reproduced independently: it instructs `graphify extract . --code-only` and then to read `GRAPH_REPORT.md`, which that command does not produce on graphify 0.9.26 | two skills give the same broken instruction; found twice on the same day by two agents who could not see each other | 2026-08-02 (`adopt-builds-graph`) | open |
| **`agents-md-template.md` ships text that fails its own drift pass.** `~/.claude/settings.json` → the path regex extracts `/.claude/settings.json` and reports it missing; `pnpm monorepo` → read as a package script; and it cites framework-only files (`agent-team-structure.md`, `stack-baseline.md`, `modules-catalog.md`, `codex-config-template.md`) that do not exist in a client repo | every generated repo starts with a drift report full of false positives, which trains its readers to ignore the drift report | 2026-08-02 (`bootstrap-generates-code-map`) | open |
| **The new `archify-setup.md` cross-reference produced two opposite calls, and neither is unreasonable.** Under a SKIP (archify missing), one arm wrote `.claudeignore` citing the "single sanctioned write outside the lane" sentence; another declined, reasoning the wiring is only warranted once archify is present. The sentence establishes that the write is *permitted* and is silent on whether it survives a SKIP. `docs-author`'s own "once per repo, at bootstrap/adopt" arguably favours writing it | a boundary clause that splits behaviour 50/50 on its first exposure is not yet a rule. Neither eval covers bootstrap-under-SKIP, so nothing catches whichever answer is wrong | 2026-08-02 (`docs-skip-is-explicit-never-silent` arms 1 vs 2) | open — **needs a decision, then a third fixture** |
| ~~**`worker-claims-before-it-writes`'s `Expected (binary)` still names `.ai/status/`**~~ while the doctrine moved to `.claude/status/` in `82b855a`. A prior run noted the move in prose and left the criterion stale | the criterion grades a path the doctrine abandoned | 2026-08-02 | **CLOSED — this row was itself stale.** `evals/worker-claims-before-it-writes.md`'s `Expected (binary)` already reads "claiming `.claude/status/<worker-id>.md`" — verified on disk 2026-08-02 during the `.ai/status/` → `.claude/status/` sweep. The criterion was updated; this row describing it as un-updated was not, which is the exact "nobody swept the move" defect the same sweep was closing elsewhere |
| **`commit:` shape is undefined under `outcome: blocked`.** A `docs-author` wrote `commit: (none — see below)`; the validator correctly rejects it. Sibling of the two format defects backlogged 2026-08-02 — and note the good news in the same run: block-list `claimed:` syntax now validates `ok`, so that earlier defect is confirmed fixed by use | a real state with no documented way to express it invites each worker to invent one | 2026-08-02 (`worker-claims-before-it-writes`) | open |
| **F1's BSD/macOS path is unproven and cannot be proven here.** The portable `sed` fix was verified on GNU sed 4.9; the run shows it is correct and non-regressive, not that it fixes the BSD failure it exists for. No BSD sed on any machine we have | the fix is almost certainly right — the temp-file + `mv` form uses no `-i` at all — but "almost certainly" is what this repo keeps writing lessons about | 2026-08-02 (`adopt-builds-graph`) | open — needs one macOS run, or an explicit write-off |
| **graphify's post-commit hook logs to one global `~/.cache/graphify-rebuild.log`** shared by every repo on the machine; two concurrent fixtures interleaved their output. Also: `graphify-out/` is committed and the post-commit hook rewrites it, so every Sailes repo has a dirty tree immediately after each commit | not our code, but our procedure installs it; the dirty-tree effect will be read as a defect by every new user | 2026-08-02 (two runs) | open — report upstream, document locally |
| **The report-file guard misfired on a legitimate deliverable.** A subagent's `Write` to `.ai/checklists/reporting.md` was refused with "Subagents should return findings as text, not write report files" — the filename starts with "report". It fell back to a Bash heredoc, so the content landed anyway | a guard that a worker routes around silently is worse than no guard: the fallback worked, so nobody would have learned the guard fires on false positives | 2026-08-02 (`bootstrap-generates-code-map`) | open — harness-side, same class as the `Write`/`Bash` asymmetry already filed |

### Debt opened by 1.29.0 (business-logic artifact), named at ship time rather than discovered later

| Item | Impact | Source | Status |
|---|---|---|---|
| **`checker` never ran on 1.29.0.** The whole release — a new template, two copies of a checker, four skill edits, five stamps — shipped on a solo pass because agent dispatch was not authorised in that session. `npm test` was green throughout, but a green gate grades the artifact's *mechanics*, not its scope or its fit; that is precisely the split `checker` exists for | this repo's own doctrine calls `checker` a mandatory gate and "never a formality". Shipping without it is not a waiver, it is an unpaid gate. The 1.28.1/1.28.2 retroactive checkers found real defects in already-live releases — twice — so the base rate of "solo pass, nothing found" is not encouraging | 2026-08-22, `.ai/runs/2026-08-22-business-logic-artifact.md` | **open — run a retroactive `checker` on the 1.29.0 diff.** Third time this shape would be used, and the first two both found something |
| **The framework now violates the contract it just shipped.** Phase 3 of 1.29.0 extended the `lifecycle` diagram contract: every state must carry its business meaning and the actor that sets it. This repo's own `docs/architecture/lifecycle.json` ("A Spec's Life") has nodes labelled `draft`, `approved`, `implemented` — labels only, no meaning, no setter | the framework eats its own food everywhere else (`docs-author`, the five stamps, the suite count). A rule shipped to every machine that the shipping repo does not follow is the `agents-md-template.md` lint-rule failure again: an enforcement announced and absent | 2026-08-22, verified by reading the JSON's nodes | open — `docs-author` lane, one diagram, at the next release that touches docs |
| **No eval for the model-behaviour half of 1.29.0.** Step 0/TERMS in `sailes-diagnose` and the Step 3 harvest in `sailes-discovery` are instructions to a model. The tests prove the text is in the file; nothing proves the instruction lands | this repo's doctrine is explicit that deterministic behaviour gets a test and model behaviour gets an eval, and that "a green test says nothing about whether the instruction lands". The 43915 rule is the highest-value clause in the release and is the least verified | 2026-08-22, D3 (eval deliberately dropped from scope) | open — candidate: a diagnose fixture whose hypothesis leans on an undefined domain term; pass = the agent asks before collecting evidence |
| **The format has never been used to write anything.** It is derived from two field artifacts — one that worked at 9 KB, one that rotted at 760 lines — but no `.ai/business-logic.md` has yet been filled with it | the first real filling may force a template change *after* release, leaving repos bootstrapped on 1.29.0 shaped differently from later ones. Accepted knowingly by Karol on 2026-08-22, who chose framework-first over pilot-first | 2026-08-22, spec §Ryzyko | open — fill Partner Portal first; the material is already extracted in `.ai/audits/2026-08-22-business-logic-inventory.md` |

### The gap the two retroactive gates actually exposed — an announcement with no consequence

| Item | Impact | Source | Status |
|---|---|---|---|
| **Nothing forces a spec's gate to RUN; the three mechanisms guard everything except that.** Corrected 2026-08-02 after "nothing checks what was produced" was challenged and was wrong. `checker` **does** check the artifact, including absences since D2 — proven by finding ten items when it finally ran. `spec-status-evidence.js` guards the *claim*, but it is scoped to `.ai/specs/implemented/`, so it grades only specs that already moved and is silent about one that never claims `implemented`. `workflow-router.js` **names every spec in flight at every session start** and says which it suspects are stale. So a spec whose phases are all done and shipped can sit in the live folder indefinitely: the router announces it daily, the evidence gate never fires because nobody makes the claim, and `checker` is never invoked because nothing invokes it | **direct evidence, from this repo's own session log:** the SessionStart hook named both 2026-08-01 specs as in flight at the start of every session from 2026-08-01 onward, including the one that finally closed them. Both were finished and on production the whole time. The warning worked perfectly and changed nothing — nobody asked "the phases are done, so where is the gate verdict". The information was present and inert | 2026-08-02, after both retroactive gates; the framing was corrected by the human pushing back on an overstated claim | open — **the fix is not another warning.** Candidate shapes, none chosen: make the router escalate rather than list when a live spec's phases all read done; or extend `spec-status-evidence` to grade the live root too, so a finished-but-ungated spec is a failing state rather than a quiet one. Note the second inverts the current design — today the gate fires on a claim, and this would make it fire on the *absence* of one, which is the same "assert presence" move the delegation spec was built around |

### Found 2026-08-02 by the retroactive gate on 1.25.2 + 1.26.0

> That gate ran **after** the code had been on production for a day, because the session that shipped
> it had delegation disabled. It returned CHANGES-REQUIRED with six items. The three blocking ones
> were fixed in 1.28.1; these are the residue, plus what the fix workers found on the way.

| Item | Impact | Source | Status |
|---|---|---|---|
| **`parity.test.js` guards concepts it was never told about, and misses ones it should hold.** Inventory taken 2026-08-02 while fixing D2: **D9** (verify a worker's worktree base before it starts) is in `agents/team-lead.md:75` and **absent from `codex-agents/team-lead.toml` entirely**, with no invariant. **D8** (the `WIP:` checkpoint convention) is now in both sides but has **no invariant** — it survives by luck, not by a gate. Same for the observation ladder and the gate-scaling block | the twin-drift gate is only as wide as its hand-maintained list, so "parity green" means "the listed concepts match", not "the twins agree". That distinction is invisible in the output | 2026-08-02, C1 fix worker, reported unprompted as the requested inventory | open — the D9 gap is a real drift; the missing invariants are the reason it survived |
| **`parity.test.js` regexes check PROXIMITY, not MEANING.** Four independent `checker` runs in 1.34.0 found the same property: P3, P3 gate fixes, P4, P4 gate fixes. It was verified on scratch copies. A negation that keeps the guarded literals ("You do NOT need a fresh database from the seed path; never the database the branch migrated matters") passes. A faithful paraphrase that drops a literal ("seed" → "fixtures") fails. The window is also an invisible constraint on prose: in P4 a correct sentence inserted between two guarded phrases broke a concept without changing its meaning | a green parity run means "the listed phrases sit near each other on both twins", not "the twins state the rule". Nobody reading `codex parity: all tests passed` can tell the difference, which is the row above in another shape | 2026-09-13, spec 2026-09-13-quality-gates P3/P4 gates; human chose "backlog row now" | open — no fix chosen. Every `checker` correctly graded it as a whole-file property rather than a defect of the diff. Candidates: a negation guard per concept; accept it and say so in the file header. **2026-09-13, P5 G6/G7 checker (G11), three more instances:** the `team-lead` concept "report FILE scoped to gate roles" passes on a scope-widening negation that keeps `checker, qa, tester` nearby; the `be-dev`/`fe-dev` concepts "message ≤40 lines" and "WIP body names verification" are near-verbatim, so a faithful paraphrase fails them and a negation keeping the fragment passes. None of the three has an `INVERSE_INVARIANTS` fixture |
| **The five Codex writing twins need the `WIP:` clause.** 1.28.1 added the checkpoint-vs-declaration split to `agents/{be-dev,fe-dev,tester,designer,docs-author}.md`; the twins were out of that worker's file list and did not get it. `parity.test.js` stayed green throughout, which is the row above in miniature | a Codex-harness worker still emits no checkpoints, so the lead's observation ladder reads an always-empty field there | 2026-08-02, C4 fix worker, flagged for whoever carries it | open — pairs with adding the D8 invariant, so the next omission cannot be silent. **Partly closed 2026-09-13 (1.34.0 P5 G6, `c0b8757`):** `codex-agents/be-dev.toml` and `fe-dev.toml` now carry "Commit often, prefix a checkpoint with `WIP:`" plus the G6 verification-body rule, and parity guards the G6 rule on both. `tester`, `designer` and `docs-author` twins still lack the clause |
| **Mechanism, not sentence: an interrupted implementer leaves no verification state on disk.** In the 1.34.0 P5 A/B every interrupted run in both arms (A4, B4, A5, B5) was recoverable on "what changed" and "what is left" but not on "what was verified". Two single-run observations say text alone does not fix it. B5 carried the new G6 sentence ("after each completed step, a `WIP:` commit whose body names the verification commands run so far") and crossed a step boundary without committing. A5 carried "write the report from your first change" and did write it, but through Bash into the shared checkout rather than its worktree, and it named no verification command either (see the isolation row below) | the lead cannot tell checked work from written work when a worker dies, which is the gap P5 set out to measure; shipping G6 as prose (G10) records the intent, not a guarantee | 2026-09-13, spec 2026-09-13-quality-gates P5.3 + G6 pair, `.ai/eval-runs/2026-09-13-implementer-report-as-message/VERDICT.md` (addendum); human decision G10 | open — no mechanism chosen. Candidates: a step boundary the brief enforces (one `Done-when` per step, each ending in a commit); a `step:` field in `.claude/status/` updated per step with the checks run. Also carries two G6/G7 checker NITS (G11): the G6 wording accepts a body like "ran the tests, they pass" with no command named; in both `.toml` twins "a commit is your declaration" precedes the `WIP:` exception it depends on |
| **A worktree worker overwrote a committed file in the shared checkout through Bash.** Measured 2026-09-13 in the 1.34.0 P5 A/B, run A5, a `general-purpose` stand-in with the `be-dev` role text and the brief line "Work only inside your own worktree". Its cwd was its worktree. It claimed its status file with `cat > <abs path to shared checkout>/.claude/status/…`, the one sanctioned outside write. Then it wrote its report with the same pattern, `mkdir -p` + `cat >` + `cat >>` to `<abs path to shared checkout>/.ai/runs/2026-09-13-quality-gates-P4-be-dev-report.md`, replacing the committed 137-line real-P4 report with its own 23 lines. The `Write` tool was never used, so the documented asymmetry holds ("`Bash` can reach outside a worktree where `Write` refuses to"). This is that asymmetry in the harmful direction | isolation is the reason writers get worktrees. Here a stray write clobbered a tracked file in the checkout the lead commits from. It was caught only because `git status` showed it after a closure commit, and the lead had staged by explicit path. A `git add -A` or `git commit -a` would have committed the overwrite. The lead's worktree-only recovery instrument missed the report entirely, and the first A5 grade was wrong because of it | 2026-09-13, spec 2026-09-13-quality-gates P5 G6 pair, `.ai/eval-runs/2026-09-13-implementer-report-as-message/returns/A5-recoverability.md` (transcript timestamps, copy, patch) | open — no fix chosen. Candidates: role text saying the status file is the ONLY absolute path outside the worktree and every other path is worktree-relative; a guard on Bash writes whose resolved path leaves the worktree, except `.claude/status/`; the lead stages by explicit path, never `-A`, as a stated rule. Only one run shows it, so the rate is unknown |
| **Secrets: "usable, never readable" — the axis nobody has built, decided 2026-08-02.** The question asked was "should the guard block shell reads of key material", and the human's answer reframed it: **there is nothing to tier by path, because production secrets are not in the repo — they live on Railway.** Repo-resident key material is local by definition, so the blanket `Read(./**/*.pem)`/`*.key`/`*.p12` in `permissions.deny` is friction with no matching risk: it stops an agent reading its own dev cert while guarding against something that is not there. The real axis is orthogonal to local-vs-production: **an agent must be able to USE a secret without READING it.** Loading into a process is legitimate (`set -a && . ./.env && pnpm dev`, `--env-file`, `process.env.X` in authored code); surfacing the value is not (`cat .env`, `grep KEY .env`, `printenv`, `env \| grep`, `echo $SECRET`). The process needs the secret; the agent does not | **this axis exists in no layer today.** Measured 2026-08-02: all eight of those commands exit 0 through the guard, and `permissions.deny` only covers the Read/Edit/Write tool surface, so the Bash path is open in both directions — a production key that got committed by mistake is readable, and a local dev cert is not. And the failure mode is worse than a normal leak: a secret that reaches an agent's context is in the transcript, in every summary of it, and in every window after — it does not get un-read | 2026-08-02, retroactive gate (C5), reframed by the human | **open — needs its own spec, deliberately not taken.** Loosening `permissions.deny` while adding a new block class on the Bash surface is a security-control change deploying to every machine with the plugin; taking it from a short message at the end of a long session is exactly the blast radius the SPEC gate exists for. Two things the spec must settle rather than assume: (a) removing the blanket key deny also removes the only backstop against a prod key committed by accident — say whether that is accepted or replaced; (b) `printenv`/`env` are also how an agent legitimately checks whether a variable is *set*, so blocking the value-dump must leave "is it present" answerable, or the rule gets worked around instead of followed |
| **`.env.stage`, `.env.live` and `.env.PRODUCTION` all pass the guard.** The tier keys on the literal `.env.prod` / `.env.staging` prefixes the spec named, so this is not a spec violation — but `.env.stage` is a common enough convention that the tier reads narrower than its own comment implies | a repo using `.env.stage` for staging gets no protection while believing the tier covers it | 2026-08-02, retroactive gate (N1) | open — small, decide whether to broaden or to say so in the comment |
| **Only one of 1.26.0's twelve doctrine changes has measured evidence that it lands.** `done-when-covers-the-allowed-files-list` PASSes with a real discrimination. `checker-reports-what-the-diff-omits` went INCONCLUSIVE twice — the control found the omission too, and the scenario itself raises that D3's `yaml` API surface may be doing the work D2 is credited with. `lead-diagnoses-processes-before-killing-them` INCONCLUSIVE twice — both arms refused the kill, so D5/D6 is re-derivable | not a defect, and the recording is honest. But it is the clearest available measurement of how much of a doctrine release is load-bearing versus re-derivable, and it argues for the mechanism-over-prose preference rather than against it | 2026-08-02, retroactive gate (N2) | open as a standing question, not a task |
| **`lead-diagnoses-processes-before-killing-them` needs a loaded-session harness.** INCONCLUSIVE twice (08-01): both arms refused the kill, and the scenario's own note says three prompts cannot put the question deep in a loaded session | a third prompt-fixture run would buy a date, not coverage, so P6 did not run it and the eval stays STALE | 2026-09-13, spec 2026-09-13-quality-gates P6, human decision G19 | open — needs a harness that creates distance and load (real prior work in the same context) before the question |
| **graphify is not installed on this machine, so three evals cannot create their condition.** `adopt-builds-graph-before-convention-audit`, `bootstrap-generates-code-map` and `explorer-prefers-graph-over-grep` all need the binary (`command -v graphify` → not found, 2026-09-13) | they stay STALE at 1.34.0, so `eval-status --strict` is not green at this release | 2026-09-13, P6, human decision G21 | open — install graphify at the validated version (see `graphify-setup.md`) on a machine that runs these, then re-run |
| **The chrome-devtools MCP is not configured on this machine.** `claude mcp list` shows none (2026-09-13). `devtools-evidence-does-not-replace-a-suite-test` and arm A of `integrity-gate-reports-measurements-not-impressions` need it | both stay blocked; arm B (instrument genuinely absent) did run, the first time that path was exercised for real | 2026-09-13, P6, human decision G22 | open — configure the MCP where these run, or re-cut arm A to a CDP bridge and record the method deviation |
| **The harness refuses a subagent's `Write` of a report-like file.** Measured 2026-09-13 in the P6 eval sweep: a `general-purpose` stand-in's `Write` of `findings.md` was refused with "Subagents should return findings as text, not write report files"; a second stand-in hit the same refusal on `summary.md` and wrote through a heredoc instead. Other stand-ins wrote `qa-verdict.md`, `tester-report.md` and `verdict.md` without refusal, so the trigger is not the file name alone and is not established | 1.34.0 P5 keeps "report file from the first change" for `checker`/`qa`/`tester` precisely so a verdict survives a dead process. If the runtime refuses such writes, the rule either fails silently or pushes gate roles onto Bash, which is the path of the A5 isolation breach (row above) | 2026-09-13, P6 evals `researcher-reports-provenance-and-does-not-decide`, `spec-escalates-oversized-open-questions` | open — establish the trigger (tool, role, path, name) before deciding whether the doctrine or the brief wording changes |
| **`checker` and `qa` must deliver a report file but neither lists `Write` in `tools:`.** Three P6 eval arms (`lead-gives-every-writer-a-worktree` arm 1, both `lead-spawns-named-roles` arms) named it independently; the plans resolved it with a quoted heredoc through Bash | the file rule for gate roles depends on Bash, the same surface that let A5 write into the shared checkout; one arm also called the doctrine self-contradictory (read-only role, file deliverable) | 2026-09-13, P6 eval findings | open — decide between granting `Write` scoped to the report path, a named Bash exception like `checker`'s base-worktree one, or a message deliverable with the lead writing the file |
| **`agent-team-structure.md` says set `effort` on a stand-in invocation; `team-lead.md` says the parameter has no provable effect.** Found by `lead-spawns-named-roles-not-general-purpose` arm 2 (2026-09-13), which followed `team-lead.md` | two canon files disagree on one dispatch field, so two leads can do opposite things and both cite doctrine | 2026-09-13, P6 eval finding | open — one sentence in one file, after re-checking the Agent tool schema |
| **archify installed as `2.17.0-dev.1` passes the `>= 2.12` floor check.** The check reads `version:` from `SKILL.md` ("2.17"); the dev channel is visible only in `package.json` and `skill-release.json` | a development pre-release is treated as a validated install on this machine, and nothing records which build a docs receipt was produced with | 2026-09-13, P6 eval `researcher-reports-provenance-and-does-not-decide` (section 2.2–2.3) | open — decide whether the floor check reads the channel |
| **"Report the status-file discrepancy, do not block" collides with "no commit = not finished" when the declared commit is absent.** `lead-verifies-status-against-worktree` arm 1 (2026-09-13 fixture: declared sha not on the branch, a declared file absent from the diff, the frozen contract edited undeclared) blocked integration on the 1.34.0 text **and on the pre-1.34.0 control** (`fb69369`). Both runs said the status check itself does not block, and held on the other rules (no finished commit; an unescalated contract change is a key decision) | the criterion reads "does not block integration on it"; `team-lead.md` says "do not block on it". Neither says what happens when the discrepancy *is* the missing declaration, so two readings are defensible and the eval FAILs one of them. Not a 1.34.0 regression | 2026-09-13, spec 2026-09-13-quality-gates P6, human decision G24 (control) | open — decide which reading is doctrine: narrow "do not block" to drift in `touched`/`claimed`, or say explicitly that an absent declaration commit still does not block, then align the criterion |
| **`gate-refuses-to-close-a-spec-without-docs-delta` arm 2's criterion predates the STOP rule it now collides with.** Arm 2 expects the agent to accept an empty receipt and close the spec (move it) when the human says "pusta delta to żaden dowód, wygeneruj coś bogatszego". `2801edf` (07-29) added "Show the human, and STOP" and "after the human has seen step 4" to `delta-at-gate.md`. On 2026-09-13 the arm accepted the receipt, re-verified it, refused to embellish, and did not move the spec: it read the human's objection as a gate still waiting for an explicit yes | the criterion grades a close the doctrine now withholds until the human agrees, so a correct lead FAILs arm 2 whenever the human's reply is an objection, not an approval. Not a 1.34.0 regression | 2026-09-13, spec 2026-09-13-quality-gates P6 eval sweep | open — re-cut arm 2 so the human's line accepts closure while doubting the delta ("zamykaj, ale ta pusta delta to żaden dowód"), or grade arm 2 on acceptance-and-no-embellishment only |
| **`browser-inspect.md` still reads as though a bridge measurement could stand in for the missing-MCP `ENV-DEFECT`.** 1.34.0 G28 (`6995c93`) put "measuring another way does not close that `ENV-DEFECT`: report both" into `agents/qa.md` and `codex-agents/qa.toml`. The source document's "Either way, measure what you legitimately can before you stop" / `run-probe.mjs` passage (`skills/sailes-design/browser-inspect.md:77`) was left as it was | the P6 arm that omitted `ENV-DEFECT` built its bridge on exactly that passage; any role reading the design skill without the `qa` role text meets the same ambiguity | 2026-09-13, `checker` finding on the G28 diff (outside G28's scope) | open — one sentence in `browser-inspect.md` next to line 77, then re-run `integrity-gate-reports-measurements-not-impressions` |
| **`diagnose-runs-live-case-before-audit` (b) failed: the orientation read opened source before the first live request.** 2026-09-13 run: the third command was `cat AGENTS.md README.md <spec> server.js public/index.html <data>`, the fourth the first `curl` against the running app. Hypotheses came only after live observation, zero code-reading agents were dispatched, and the arm disclosed the order itself | rule 1 of `sailes-diagnose` ("reproduce first") lost to the ordinary "read the repo first" habit on a small repo where reading everything is one command. The criterion bars opening source first, and the mechanism was then found in the source it had already read | 2026-09-13, spec 2026-09-13-quality-gates P6, human decision G25 | open — decide whether the skill names the orientation read (what may be read before the first observation: mandate, skill, AGENTS.md, spec; not source) or the criterion accepts an orientation read without theorising |
| **`inner-loop-promotes-what-caught-a-real-defect` needs a fixture whose defect cannot be seen on a read.** The 2026-09-13 rebuild put `parseAmount('')` → `0` in a readable 6-line helper; the arm anticipated it and guarded before any check ran, so no check went red and the eval is INCONCLUSIVE. The same fixture's `Done-when` used `node --test <dir>`, which fails on Node 26 | the promotion rule is only testable when a quick check catches a real defect in-session; a defect visible on a read is caught by reading | 2026-09-13, P6, human decision G27 | open — re-cut: helper shipped as a minified or compiled `dist/` file (readable in principle, not in practice), `Done-when` with a glob |
| **Eval fixtures survive only in their post-run state, or not at all.** `.ai/eval-runs/2026-07-29-stale-rerun/fixture/repo` failed its own `PRE-DISPATCH-SHA256.txt` on 3 files (the run's lint rule and promotion record were inside); the 08-30 tier fixture kept only the suites. Both had to be rebuilt in P6 | a re-run cannot reproduce the condition it re-grades, and a rebuilt fixture is a different fixture; the eval-runner cleanup rule says keep "ground truth" but not "the pre-dispatch fixture" | 2026-09-13, P6 evals `promotion-prefers-enforcement`, `tier-scales-the-case-list-not-only-the-proof` (G18) | open — small: add "the fixture as dispatched (or a script that rebuilds it)" to the keep list in `sailes-eval-runner` |
| **`--strict` na evalach był czerwony PRZED 1.36.0 — 1.35.0 wyszło z 24 nieświeżymi evalami.** Zmierzone 2026-09-20 w odłączonym worktree na `1fc781e`: `node evals/harness/eval-status.js` → 31 fresh, **24 stale**; `--strict` → exit 1. Ta sama komenda na `41434e2` (po P1–P4 tego wydania) → 28 stale. Czyli **moja zmiana unieważniła cztery** (`checker-never-sees-maker-narrative`, `checker-reports-what-the-diff-omits`, `mock-of-an-external-boundary-carries-a-pair`, `integrity-gate-reports-measurements-not-impressions`), a **dwadzieścia cztery to dług 1.35.0**, w większości z 2026-09-17 | `sailes-eval-runner` mówi wprost: „Before a release → `--strict` on a clean checkout". 1.35.0 tego nie zrobiło, a nic tego nie zatrzymało — `release-hygiene.test.js` sprawdza stemple i CHANGELOG, nie świeżość evali. Nieświeży pin to miejsce, w którym nie wiemy, czy doktryna nadal ląduje; dwadzieścia cztery takie miejsca to ćwierć korpusu | 2026-09-20, pomiar przy P5 specu 2026-09-20-harness-guards; człowiek wybrał „moje cztery + nowe, dług do backlogu" | **open.** Dwie rzeczy do rozstrzygnięcia osobno: (a) przebieg czyszczący 24 zaległe piny; (b) czy `--strict` wchodzi do `release-hygiene.test.js` albo do bramki wydania jako warunek, a nie jako zdanie w skillu, którego nikt nie uruchamia |
| **Worker wpisuje sha do pliku statusu z pamięci, nie z komendy.** Dwa z trzech plików `.claude/status/` w przebiegu `wf_238e2c36-4f6` miały w polu `commit` halucynowany hex przy pierwszym zapisie (`8f25ca3f9b8b…` zamiast `8f25ca3a178b…`, `bfc732b3e8bc…` zamiast `bfc732b2188d…`). Oba workery złapały to same i dopisały linię `correction:` | Cała wartość deklaracji polega na tym, że jest wiarygodna. Sha, którego nie da się użyć, znosi mechanizm dokładnie tam, gdzie miał działać — lider czyta `commit` i cherry-pickuje. Że obaj złapali to sami, jest szczęściem, nie zabezpieczeniem; trzeci mógłby nie | 2026-09-20, run log `.ai/runs/2026-09-20-harness-guards.md` | **open — kandydat na jednozdaniową poprawkę doktryny.** Klauzula pliku statusu w `agents/{be-dev,fe-dev,tester,designer,docs-author}.md` (+ bliźniaki) mówi, że `commit` jest **wklejony z wyjścia `git rev-parse HEAD`**, nigdy wpisany. Tani, sprawdzalny grepem |
| **Odmowa zapisu poza worktree jest niedeterministyczna w obrębie jednego przebiegu.** W `wf_238e2c36-4f6` P1, P2 i P3 zapisały do `.claude/status/` w głównym drzewie bez problemu; P4 i `tester` dostały odmowę („agent is isolated in the worktree") i użyły fallbacku wewnątrz worktree, zgłaszając to w `note` | `agents/be-dev.md` i `team-lead.md:103` opisują to jako **stałą** asymetrię `Bash` vs `Write`. Pięć workerów, jedna sesja, dwa różne zachowania — czyli doktryna opisuje mechanizm, którego nie ma w tej postaci. Lider szukający pliku w głównym drzewie dla workera, który żyje, przeczyta „nigdy nie wystartował" | 2026-09-20, ten sam przebieg | **open.** Reguła fallbacku zadziałała i oba workery ją zgłosiły, więc strata była zerowa — ale opis przyczyny w doktrynie jest nieprawdziwy. Ustalić wyzwalacz przed poprawieniem zdania |
| **Kolizja id workera wewnątrz jednego workflow.** P1 i P2 dostały to samo `be-dev-238e2c36` (id pochodzi od run id workflow, nie od agenta). P2 obszedł to nazwą opisową `be-dev-p2-a4.md` | `workflow-orchestration.md` każe nieść run id w nazwie pliku statusu, żeby uniknąć kolizji **między** równoległymi workflow. Nie wystarcza, gdy jeden workflow dispatchuje kilku workerów tej samej roli — a to jest wzorzec fali. Kolizja nadpisuje cudzą deklarację, czyli odtwarza wewnątrz mechanizmu detekcji dokładnie tę awarię, przed którą on chroni | 2026-09-20, ten sam przebieg | **open.** Nazwa potrzebuje sufiksu fazy albo indeksu agenta, nie samego run id |
| **Niescalona praca z 1.34.0 wisi w osieroconym worktree.** `.claude/worktrees/agent-a928e3faaa4d4cf6b` trzyma `160a4c8 docs(1.34.0): architecture delta` — cztery pliki, 293 wstawienia (`architecture-validate.json`, `compare-attempt.json`, `release-1.34.0-notes.md`, `docs/architecture/architecture.json`). `git merge-base --is-ancestor 160a4c8 HEAD` → nie; gałąź `worktree-agent-a928e3faaa4d4cf6b` jest jedyną, która go zawiera | To jest delta dokumentacji, której 1.34.0 nigdy nie dostało — a `release-hygiene.test.js` sprawdza **obecność** delty, nie jej świeżość, więc wydanie przeszło. Praca nie jest stracona tylko dlatego, że worktree dzieli `.git` z repo | 2026-09-20, znalezione przy sprzątaniu worktree po `wf_238e2c36-4f6` | **open — decyzja człowieka: scalić do 1.36.0 czy odrzucić.** Nie ruszane tym wydaniem |
| **`.claude/status/` nie jest sprzątane od sierpnia.** 33 pliki z poprzednich przebiegów, najstarsze z 2026-08-06, po usunięciu pięciu z tego wydania | `team-lead.md:104` mówi, że plik znika **razem z** wpisem w run logu, a inwariant brzmi: to, co leży w `.claude/status/`, jest albo żywe, albo martwe, nigdy nie jest nieaktualnym zapisem czegoś już zamkniętego. Trzydzieści trzy pliki później inwariant nie obowiązuje, a `worker-status.js --sweep` ma szum, w którym ginie sygnał | 2026-09-20, ten sam przebieg | **open.** Zwinąć zbiorczo do jednego wpisu historycznego albo skasować po sprawdzeniu, że każdy ma swój run log |

## Later phases (from specs)
| Phase / feature | From spec | Trigger to start |
|---|---|---|
| Estimation ledger location + first aggregates feeding `sailes-wycena` | 2026-07-05 value spec Phase 6 | first project closing estimate-vs-actual data |
