# Eval: bootstrap generates the code map (graphify) and proves it on disk

Skill under test:   `sailes-bootstrap` (Step 4.9 + `graphify-setup.md` + `repo-done-checklist.md`)
Files:              skills/sailes-bootstrap/SKILL.md, skills/sailes-bootstrap/graphify-setup.md, skills/sailes-bootstrap/repo-done-checklist.md
Setup:              Give a fresh subagent the sailes-bootstrap skill and a Case B task ("bootstrap
                    this empty repo for a small B2B tool; stack already confirmed as the baseline").
                    The machine has `graphify` on PATH. Observe the steps it announces/runs and the
                    final checklist output.
Expected (binary):  Before handoff it (a) runs `graphify extract . --code-only`, (b) runs
                    `graphify hook install`, (c) runs `graphify claude install` AFTER
                    `.claude/settings.json` exists, and (d) the verification block reports
                    `graphify-out/graph.json` + the post-commit hook as OK (or an explicit SKIP
                    line naming the missing binary — never silence).
Failure looks like: Bootstrap finishes with no `graphify-out/`, no git hook, and the done-checklist
                    never mentions the code map (the pre-1.11.0 baseline: graphify was not part of
                    the framework at all).
Last run:           2026-08-02 (at b6f8b04) · **PASS** · stand-in vehicle (general-purpose subagent pointed at the WORKING-TREE skill files; grades the TEXT, not runtime pins). The real-plugin vehicle was rejected on evidence, not preference: `~/.claude/plugins/marketplaces/sailes` sits at d6b64e2 while local `main` is 10 commits ahead and unpushed, so the deployed copy still carries the pre-fix `sed -i -E` and the hardcoded `.git/hooks` check — a plugin run would have graded the defects, not the fixes. Ran FOR REAL in an empty fixture repo (scratchpad, 5 commits): extract → hook install → claude install with `.claude/settings.json` already committed at cf33018 (graphify appended PreToolUse and left all 13 `permissions.allow` entries and SessionStart intact — merge, not overwrite) → codex install → portability fix → ignore wiring → commit. Grader checked disk, not the report: `graphify-out/graph.json` committed (321 nodes), `.git/hooks/post-commit` marker-delimited and actually firing (graph.json mtime == the last commit’s timestamp), and the checklist block re-run by the grader printed `OK graphify-out/graph.json` / `OK .claudeignore covers graphify-out/` / `OK freshness hooks (post-commit, …/.git/hooks)`. Both 2026-08-02 fixes exercised: **F1 GREEN** — the installers really did write `C:/Users/karol/.local/bin/graphify.EXE` into both files (confirmed in a pre-run smoke repo), the temp-file+`mv` form removed it, and the verification grep did not match; **F2 GREEN, half by the run and half by probe** — the bootstrap only hit the unset-`core.hooksPath` branch (`.husky/` was scaffolded but never activated, since no install ran), so the husky branch was probed separately: with `core.hooksPath=.husky` graphify installs to `.husky/post-commit`, `.git/hooks/post-commit` does not exist, and the block reports OK — the 2026-07-28 false MISS is gone. NOT covered: dependency install was out of scope by grader constraint, so boot was never established. Two new INSTRUMENT defects, reported not fixed: `skeleton.md` says a committed `GRAPH_REPORT.md` ships, but the Step 4.9 procedure never runs `graphify cluster-only` and so leaves the graphify-installed AGENTS.md section pointing at a file that does not exist; and this checklist’s prose table requires `.ai/adr/ADR-001-*` while its script checks only `.ai/adr/template.md`, so a bootstrap that skipped the first ADR passes green.
Prior run:          2026-07-28 · **PASS** · stand-in vehicle (general-purpose + working-tree text; grades the TEXT, not runtime pins), and this arm ran the steps FOR REAL in a fixture repo: extract → hook install → claude install after .claude/settings.json existed → codex install → portability fix → ignore wiring → commit; graph.json + settings wiring verified on disk by the grader; checklist verification block pasted in REPORT.md. Two INSTRUMENT defects found and backlogged, neither a doctrine miss: the checklist's hook check hardcodes .git/hooks/post-commit and false-negatives on husky-managed repos (hooks WERE installed and fired), and graphify-setup.md's `sed -i -E` is GNU-only — silently no-ops on macOS, the house silent-success shape on this very platform.
