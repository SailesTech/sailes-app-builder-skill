Working tree clean, final commit `70e4cf4` is the declaration of done.

## Report

Phase P4 ("zastana czerwień ustalana na bazie") is complete, committed at `70e4cf4` in worktree branch `worktree-agent-ad3f12359e91fac57`, cut ff-only from `738be36`.

**Files changed** (all under `/home/charlie/Work/Internal/sailes-app-builder-skill/.claude/worktrees/agent-ad3f12359e91fac57/`):
- `skills/sailes-implement/SKILL.md` — Pre-push gate paragraph + Pre-flight step 3 (P4.1)
- `skills/sailes-bootstrap/release-checklist.md` §0 checklist lines (P4.1)
- `agents/qa.md` + `codex-agents/qa.toml` — red-by-name-vs-merge-base procedure (P4.2)
- `agents/checker.md` + `codex-agents/checker.toml` — same procedure for phase `Done-when`, base = brief's ff-only cut point (D-P4a), plus the named read-only exception (D-P4b) (P4.3)
- `codex-agents/parity.test.js` — one new INVARIANTS concept each for `qa` and `checker`
- `evals/gate-compares-red-by-name-not-count.md` — new eval (P4.4)
- `.ai/runs/2026-09-13-quality-gates-P4-be-dev-report.md` — full report, written incrementally from the first change

**Done-when — all pass**: `grep -nE 'merge-base'` hits all 4 files; `grep -n 'comm -23'` hits both; `node codex-agents/parity.test.js` exits 0 with both new concepts green; `node tools/sync-blocks.js --check` in sync; `npm test` exits 0, 0 `not ok` across all 22 suites. Full pasted output is in the report.

**Mutation proof** (parity concept) done on a scratch copy outside the repo, never touching tracked files: stripping the concept sentence from `qa.md` alone → exit 1, exactly the `qa` concept fails; restored → exit 0. Same for `checker.md` → exit 1 on exactly that concept; restored → exit 0.

**Ambiguities flagged in the report, not decided by me**:
1. The parity regex uses a single generously-bounded `{0,400}` window rather than two independently-required regexes — a smaller diff but less robust to future wording drift; noted as a possible follow-up.
2. Spec wording ("wkleja comm -23...") was read as "always paste the diff output, non-empty triggers CHANGES-REQUIRED" rather than "only surface it when non-empty" — flagged in case the narrower reading was intended.
3. `release-hygiene.test.js` still reports "five stamps at 1.33.1" — expected, since version bumps are P6's job, not P4's.
4. Eval file's column alignment was matched by eye against the two reference evals.

Status file `.claude/status/be-dev-P5ab-A1.md` (written via Bash, since `Write` refuses paths outside the worktree, per the role's documented fallback) is claimed and closed with `outcome: done`.
